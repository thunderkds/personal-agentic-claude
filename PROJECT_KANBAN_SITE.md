# PROJECT_KANBAN — Kit Landing Site (v1)

> Separate board per Hard-Stop Gate 4: the static site deploys to Vercel, a different target from
> the harness itself. Harness tasks live on `PROJECT_KANBAN.md` and are never mixed in here.
> Spec: `PROJECT_SPEC_SITE.md`.
>
> Task IDs stay in the `Txxx` namespace (shared, monotonic across both boards) because
> `pre_agent_validate_guide.py` and `lib/task_context.py:resolve_task_id` both parse that exact
> pattern. A `S001`-style ID would silently defeat spawn validation.

## Todo

- [ ] **T087** — **The slimmed README points at site content that does not exist.** Registered 2026-08-21 from T085's Stage 4 review; user chose "extend the site" over keeping the sections in the README or pointing at in-repo docs. T085 cut `README.md` 477 → 55 lines and told readers the full reference "lives on the project site" — measured against `site/index.html`: `pack` 0 hits, `Memory System` 0, `Codebase Map` 0, `Repository layout` 0, `brownfield` 0, `SUPERVISOR_PATH` 0. README line 38 sends someone installing a pack to the site for "the full Quick Start, Options table, and Update flow", none of which are there; ~400 lines of documentation exist only in git history. **Root cause is a Supervisor Stage 2 planning conflict, not an implementer error**: T083's cut list deliberately dropped the pack matrix from the page ("keep v1 to the four rosters") while T085's cut list assumed the site would carry it — both written by the Supervisor, neither reconciled. The implementing agent's AC3 report did assert all of it "confirmed present on site/index.html" after spot-checking the hooks table and generalising, which is a separate and real reporting defect, but the content gap was designed in before any agent ran. Scope: packs (5, from `packs/*/PACK.md` + `git show 78ebfa1:README.md`), update flow, Options table, repository layout, memory system, fork/brownfield install variants — plus **AC7, the load-bearing part**: a test asserting every reference topic the README promises is actually on the page, so a future unhonoured promise fails the suite instead of shipping. Carries three edge cases that must not be flattened: `SUPERVISOR_PATH` is packs-only and unused by the core install; the `~/.supervisor` persistent clone is still required for packs specifically; and `curl … \| sh --pack=x` genuinely does not work (sh parses its own flags first) and must not be simplified into a working-looking one-liner | Frontend-Implementer | C2 | Risk: Low | P0 | Registered 2026-08-21
- [ ] **T086** — **Mobile responsiveness follow-up on the landing page, from T083's `/verify` findings.** Four observations, none of which breach a T083 acceptance criterion — the page passes as specified — so they are tracked forward rather than reopened. (1) At 375px the **install command is truncated** to `curl -fsSL https://raw.githubusercontent` and needs horizontal scrolling inside its code block to read or select; the string is correct and selectable, but it is the page's primary call to action and a phone visitor meets it cut off, with no copy button. (2) At 375px the hook table's **`Behavior` column is off-screen** — that column carries the two corrected hook facts (`inert`, `default 90`) that T083's AC5 exists to publish, so a mobile reader must scroll sideways to reach precisely the content the task was built to correct. Stacking the table to cards under 480px would resolve (1) and (2) together. (3) **No favicon ships** — two `GET /favicon.ico → 404` per load, observed in the server access log, on a page that otherwise makes zero requests; blank tab icon on a v1 release. (4) **Desktop measure is inconsistent**: `body` caps at `960px` but `section` at `72ch` (~640px), so the hero card spans the full container while every section below stops ~290px short of it — legible, arguably a deliberate reading measure, flagged for the author's call rather than asserted as a defect. Also folds in T083's open Stage 4 **P2**: `test_no_external_assets` inspects only `src=`/`href=` attributes, so a future CSS `url()`/`@import`/`srcset` would fetch over the network with the suite green — latent today, and worth closing while the same file is open. **Also folds in a published-wrong-number defect, and a Supervisor correction.** The page states the hot-tier memory cap as `≤50,000 characters`; the **enforced** value is **45,000** (`.claude/hooks/tests/test_token_audit_format.py:91` `HOT_TIER_CHAR_BUDGET = 45_000`, and `memory/MEMORY.md`'s own header). `CLAUDE.md:194` still says 50,000 and is **stale** — the ratchet was lowered to 45,000 by **T075**, whose own memory entry records that its `/verify` "caught the seeded stub stating both 45,000 and 50,000". So this precise confusion has now been caught twice, and the second time it escaped into a public page. At T087's `/verify` the Supervisor called this "the page picked the governing doc correctly" — **that was wrong**: a ratchet's current value lives where it is enforced, not in the prose that set it originally. Fix the site's number to 45,000 (or drop the figure and point at the enforced constant), and flag `CLAUDE.md:194` for a separate harness-side correction — it is out of this task's site scope but is the root of both incidents. UI task → all three Gate 6 design Evidence rows apply | Frontend-Implementer | C1 | Risk: Low | P2 | Registered 2026-08-21

## In Progress



## Ready for Review

## Done

- [x] **T084** — **Vercel deploy configuration + runbook.** `vercel.json` pinning the static output to `site/`, no build command, plus a runbook section giving the operator the exact deploy commands. The Supervisor plans and de-risks this; **the operator executes the deploy** — no agent pushes to an external host. Depends on T083 (nothing to deploy until the page exists) | Common-Infrastructure-Agent | C1 | Risk: Low | P0 | Registered 2026-08-21 | **Done** — Stage 3 complete 2026-08-21 by `common-infrastructure` (sonnet, C1) in worktree `wt-t084` on `feat/t084-vercel`, branched from `release/v1-kit-site`. 7/7 ACs met; 693 passed (baseline 688, +5), 0 regressions. Schema form: `outputDirectory`, not the legacy `builds` array — `builds` is the older config-as-code form tied to per-build runtimes/functions, and for a directory with no build step it would imply a pipeline that does not exist. Both mandatory mutation controls independently re-run by the Supervisor, each RED naming the exact break; the T083 Stage-4 P2 carryover was also closed here (`test_no_external_assets` now covers CSS `url()`, `@import`, and `srcset`) and independently probed with a real `background-image: url(https://evil.example/x.png)`, which the extended test caught. **Stage 4 code-review: 0 P0 / 1 P1 (fixed) / 0 P2 / 0 P3.** The P1 is the one worth remembering: the implementer reported that `memory/`, `tasks/` and `PROJECT_KANBAN*.md` are *"never in the published output"* — **true, and it still left the hole**, because `outputDirectory` scopes what Vercel *serves*, not what the CLI *uploads*. The Vercel CLI transmits the project source tree to its build infrastructure on every deploy, so this repo's project memory (decisions, learnings, event traces) and every TASK_GUIDE would have left the machine on every `vercel` invocation and been retained by a third party — never at a public URL, which is exactly why neither the config nor the runbook's own "scope of what gets published" paragraph surfaced it. Fixed with `.vercelignore` as an **allowlist** (bare `*`, then re-admit `site/` and `vercel.json` only); a denylist was rejected because it fails open the first time anyone adds a directory — the same failure mode as the documentation drift this release exists to fix. Two new assertions cover the allowlist form and the never-re-admit rule, both mutation-controlled (3 further controls run by the Supervisor: denylist form, re-admitting `memory/`, and file absent — all RED). **Stage 5 user-run `/verify`: PASS, with the deploy explicitly out of scope.** No Vercel CLI was installed or authenticated and no deploy was run — deploying is outward-facing and the operator's call. What *was* driven: the real gitignore matcher via `git check-ignore` against a scratch tree, showing exactly two files survive upload (`site/index.html`, `vercel.json`) with `memory/`, `tasks/`, `docs/`, `PROJECT_KANBAN.md`, `README.md`, `.claude/hooks/` all excluded, and fail-closed confirmed under growth (new files inside `site/` admitted automatically; a new top-level dir and `.env` excluded with no edit); plus the runbook's step-4 verify command run verbatim against a served copy (`MATCH`) and a wrong page (`NO MATCH`), proving it discriminates. Recorded limitation: whether Vercel honours `outputDirectory` for a build-less project is a platform property, unverifiable here — **the operator must run the preview deploy before `--prod`**. Also recorded: the gitignore re-inclusion trap did not bite only because `!site/` precedes `!site/**`; a lone `!site/**` would be silently inert and upload nothing, so the two lines are load-bearing despite looking redundant | Completed: 2026-08-21
- [x] **T083** — **Static landing page for the kit (`site/index.html`).** One page, plain HTML/CSS, no build step, no JS framework. Sections: what the kit is, the 5-stage pipeline, agent roster (4 spawnable roles + the shared base template), skill roster (30, grouped by pipeline stage), enforcement hook table (8 hooks with their real wiring and whether each blocks or advises), install + update commands, prerequisites. Content must be derived from source files, never from `README.md` prose — the README is known to carry two false hook claims (T081). Gate 5 test is `tests/test_site_content.py`, which asserts every `.claude/skills/` dir, every agent `name:`, and every hook in `.claude/settings.json` appears on the page, and that the step-limit default quoted on the page equals `pre_agent_step_limit.py`'s `STEP_LIMIT` literal — so a future skill addition fails CI instead of shipping a stale page | Frontend-Implementer | C2 | Risk: Low | P0 | Registered 2026-08-21 | **Done** — Stage 3 complete 2026-08-21 by `frontend-developer` (sonnet, C2) in worktree `wt-t083` on `feat/t083-site`. 9/9 ACs met; 688 passed (baseline 680, +8 new), 0 regressions. TDD confirmed: the drift test was observed RED (8 failures, `FileNotFoundError` on `site/index.html`) before the page existed. All three mandatory mutation controls independently re-run by the Supervisor, each RED naming the exact break. **Stage 4 code-review: 0 P0 / 1 P1 (fixed) / 1 P2 (open) / 0 P3.** The P1 was the **Supervisor's own Stage 2 error**, not the implementer's: the Verification Command `python3 -m pytest tests/ -q` collects 8 tests, not 688 — the harness suite lives in `.claude/hooks/tests/`, which bare pytest skips because `.claude` is hidden, so an agent following the guide verbatim would have reported green having run no regression tests; corrected in the T083/T084/T085 guides. P2 open and carried to T084: `test_no_external_assets` inspects only `src=`/`href=`, so a future CSS `url()`/`@import` would fetch over the network with the suite still green (latent — the page has none today). security-review: **N/A**, Low risk, no runtime code path. Also recorded because the inverse error is this repo's recurring one: the Supervisor's first Stage 4 pass wrongly reported mutation control M2 as non-reproducible and began rewriting the assertion around it — the page reads `default <strong>90</strong>`, so the Supervisor's own `sed` matched nothing and mutated no bytes; the implementer's evidence was accurate and the rewrite was reverted rather than kept. **Stage 5 user-run `/verify`: PASS with 4 non-blocking findings.** Surface was the page served over HTTP and driven in headless Chrome, not the markup read back: the zero-network claim was verified from the **server access log** (`GET / 200` ×3, `GET /favicon.ico 404` ×2 — no CSS/font/image/script fetch), which is stronger evidence than the `src=`/`href=` test that Stage 4 flagged as incomplete. Findings carried to T086, not fixed here | Completed: 2026-08-21

## Blocked

| Task | Blocked by | Since |
|------|-----------|-------|
