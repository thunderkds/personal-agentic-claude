# PROJECT_KANBAN.md
**Last updated**: 2026-08-16

> Compact task board. Full context lives in `PROJECT_SPEC.md`. Update this file whenever a task status changes.

---

## Board

### Todo
- [ ] **T072** — `setup.sh` silently downgrades a brownfield install to greenfield when stdin is not a tty. Found by `/verify` while driving T071 at the installer surface: `printf '2\n' | sh setup.sh` prints only an `[info]` line about non-interactive mode and installs `CLAUDE.md`, not `CLAUDE_LEGACY.md` — the guard is `[ ! -t 0 ]`, which cannot distinguish "no human here" from "answers are being piped in". A brownfield user scripting their install therefore receives the **wrong supervisor rules** with no error and no non-zero exit. Reproduced both ways during T071 verification: piped stdin → `CLAUDE source: CLAUDE.md`; a forced pty (`script -qec`, answer `2`) → `CLAUDE source: CLAUDE_LEGACY.md`. **Pre-existing, not caused by T071**, and correctly left out of its scope. Likely fix is a `--brownfield`/`--greenfield` flag so the choice is expressible without a tty, with the tty prompt kept as the interactive default; the `fd0`-prompt/`fd3`-filelist split recorded in the install-era learnings is the existing precedent for making a script both promptable and pipe-drivable | common-infrastructure | C1 | Risk: Medium | P2 | Registered 2026-08-16 from T071 `/verify`

### In Progress


### Ready for Review

### Done
- [x] **T071** — **Vital Slice — extend Simplicity First into "rank the requested", in the guaranteed channel.** From the user's Pareto framing, scoped explicitly at *downstream* repos that install this kit rather than at this repo's own tidiness: at feature-delivery time build the vital slice of a feature's implementation surface and record what was deliberately not built. Stage 0.5 established the gap is real — `CLAUDE.md:111`'s Simplicity First prohibits the **unrequested** and compresses what is written ("if 200 lines can be 50"), but never says *rank the requested and decline part of it*; "do nothing" was therefore rejected on the merits, not dismissed. Three of four paths rejected in `docs/ddr/0005`: a 5th Karpathy principle (would land in `CLAUDE.md`, which is **not in the sub-agent read set**, so it never reaches the agent writing code), an optional `pareto` skill (LR-0002 records that optional steps are skipped exactly when tasks *feel* small — when the 80% accumulates), and no-op. **The Stage 2 pre-flight sweep is the headline: the brainstorm predicted 3 files, the sweep found 8 shipping locations carrying a Simplicity First statement plus 3 test pins guarding them** — the compact Karpathy table rows in all four role guides are byte-pinned verbatim by `test_agent_guide_dedup.py:KARPATHY_TABLE` **and** by `scripts/test-agent-template.sh` via `grep -qF`, and `test_memory_channel_and_budget.py` separately requires the two "If 200 lines can be 50" occurrences to survive untouched. Third consecutive occurrence of "extending a convention touches more places than the AC table enumerates" (T058, T065, T070). That finding **improved** the plan rather than derailing it: the rule goes into the unpinned `## Simplicity First (your defining constraint)` prose section *beside* the pinned table — "fix the prose around a pinned string, not the test", applied by **placement**, so AC6/AC7/AC8 are positive byte-identity assertions and no test constant is edited. User ruled four things: all four role guides (not just the two that have the section — `common-infrastructure.md` and `qa.md` have only the pinned table row, the T066 smallest-leaf shape); **per-role wording**, enforced by AC2 as a ≥12-consecutive-word overlap check; canonical term **`Vital Slice`** with the number confined to `CLAUDE.md` and file-wide-negative-tested absent from every operative file (a number with no instrument becomes a target — DDR-0001/DDR-0002/T063, three recorded instrument-validity failures); and a hard **+8 lines per guide** cap (+2 `CLAUDE.md`, +4 template, ~38 total). The cap is about **attention, not price** — T061 measured ~97% of spawn tokens as cache reads and DDR-0004 established spawn *count* as the cost lever, so these lines are ~free in dollars; they cost legibility, and T066 had just measured −4.7%/−15.4%/−15.6%/−17.0% in these exact files. **The load-bearing sentence is the counter-rule, not the Pareto framing**: a Vital Slice narrows implementation surface only — never an Acceptance Criterion, never a pipeline stage, never a Hard-Stop Gate. Without it this ships a principle-sanctioned vocabulary for the exact drift LR-0001/LR-0002 record and DDR-0004 refused to exempt. Advisory only — no hook, no gate, no backfill (T046 `Pattern reference` precedent, AC12/AC13 pin both as negatives). Stage 4 must check the **inverted-ranking tell**: a Cut List consisting mainly of error handling, validation or boundary conditions has the ranking backwards, since that is where correctness lives and is exactly the code that looks like the disposable 80% | Common-Infrastructure-Agent | C2 | Risk: Medium | P2 | Stage 0.5 `brainstorming` + `grill-with-docs` (terminology) completed 2026-08-16; 4 paths explored, 2 glossary terms sharpened into `PROJECT_SPEC.md`, DDR gate cleared 2-of-3 → `docs/ddr/0005-vital-slice-extends-simplicity-first.md` (**not** flagged ADR-eligible — criterion 1 is only partly met; ADR stays rare) | Stage 2 planned 2026-08-16; guide at `tasks/TASK_GUIDE_T071.md`, 13 ACs, 8 success criteria of which 4 are mandatory mutation controls (incl. padding the line cap with **both** blank and non-blank lines — T067's P3 was exactly that blindness) | Stage 4 2026-08-16: code-review **0 P0 / 0 P1 / 0 P2 / 2 P3** (neither applied — a ~140-char clause traded out of `common-infrastructure.md` to fit AC7's char budget, and the now-densest cell in the principles table; both accept-as-is). security-review **PASS, 0 actionable**, run manually and labelled — the built-in diffs the checked-out branch vs `origin/HEAD` and pulled in ~70 files of T059-T070 work, **8th** occurrence; correctly scoped the diff is 13 markdown files + 3 test modules, every `subprocess` call site argv-list form over module constants. **Two Stage 2 defects, both the Supervisor's, both caught before merge**: AC5 mandated a `CLAUDE.md` edit while AC6 forbade touching the test pinning `CLAUDE.md` byte-for-byte (unsatisfiable — the agent halted rather than editing a test green, twice), and the ruled +8-lines-per-guide budget was unreachable for **all four** roles (362 chars/role headroom vs ~600 needed) because the sweep checked pinned *strings* and never *size invariants*. Three byte-pins fought in one task = occurrences **4, 5 and 6** of scope-guard-as-invariant; two refinements recorded: repointing `BASELINE_REF` would have made AC7 fail *by construction* (`before == after` under a strict `<`), and `test_guide_sections.py`'s constant fed **four** assertions including T071's own no-backfill guard, so it was **split** (`APPROACH_BASELINE_REF`) rather than moved — with a mutation control run on each side, by the agent and independently re-run by the Supervisor. `verify` **pass** — driven at the **installer** surface into two fresh `git init` targets: greenfield and brownfield both carry the rule, `80/20` present exactly once in `CLAUDE.md` and zero times in any operative file, idempotent re-install clean, `harness-lock.json` covers all 5 agent guides. 37 new tests, **442 passed** (was 405) | Completed: 2026-08-16
- [x] **T070** — two pointers now name a channel that no longer holds the content. `CLAUDE.md:86` ("see the Complexity matrix in `.claude/agents/general-agent-template.md`") and `docs/claude-md/pipeline-stages.md:118` (same claim, in the Stage 2 labelling instructions) both direct the reader to a file from which T066 removed the C0–C3 matrix — it now lives in each role guide. Same class as T069 one level up: a reference that survives the move of the thing it references. Low cost, but deliberately **not** folded into T069 (Karpathy: Surgical Changes, and `CLAUDE.md` is pinned byte-identical by T066's AC5, so editing it needs its own scope and its own re-pin). Stage 2 must confirm whether `test_agent_guide_dedup.py`'s AC5 byte-identity pin on `CLAUDE.md` has to be repointed to a new baseline ref as part of the fix | common-infrastructure | C2 | Risk: Medium | P2 | Registered 2026-08-10 from T069 Stage 2 | Stage 2 planned 2026-08-13; guide at `tasks/TASK_GUIDE_T070.md`. **The row named two locations; the Stage 2 sweep found three, and the third is the most valuable.** `templates/TASK_GUIDE_template.md:18` carries the identical stale pointer in step 5 of the Mandatory Startup block — `MANIFEST:11` ships the bare `templates` directory to every downstream repo, so this is the only one of the three sitting in the **guaranteed channel of the reader it misdirects**; the other two are Supervisor-facing. Third occurrence of "retiring a convention touches more places than the AC table enumerates" (T058, T065, now T070), and the second consecutive task where the Supervisor's own row under-counted before the sweep ran. Every other hit classified and left alone: `README.md:72` and `general-agent-template.md:8` are already correct (they *describe* the move); ~35 `tasks/TASK_GUIDE_T0*.md`, `TASK_REVIEW_T066.md` and `memory/decisions.md:858` are historical record and stay byte-identical under AC7, following T064's **fallback-not-migration** precedent — back-editing them would falsify the audit trail of what each agent was actually told. **The AC5 pin question the row raised is answered: yes, it must be repointed.** `test_agent_guide_dedup.py:184` parametrizes `["CLAUDE.md", "MANIFEST"]` against `BASELINE_REF = "8fc4dd2"`, so AC1 turns it RED by construction — no correct implementation satisfies both. Resolution follows T064's rule and T069's own precedent in that same file: add a second named ref (`T070_BASELINE_REF`, mirroring the existing `T069_BASELINE_REF` at line 45) and split the parametrize, keeping the assertion **shape** byte-identical and `MANIFEST` on the old ref. Deleting the entry is forbidden — it guards T066's finding that `CLAUDE.md`'s overlap with the agent guides is cross-context redundancy, which is still true after T070. **Wording deliberately differs per file rather than one replacement applied three times**: the template addresses an implementing agent, who *has* a role guide ("in your role guide"); `CLAUDE.md` and `pipeline-stages.md` address the Supervisor, who has none, so those name the directory. Applying one phrasing to all three would repeat the exact error T066 diagnosed. **Labels raised from the registered C1/Low, stated not silent**: Risk Low→Medium by the project's own rule on the very line being edited (`pipeline-stages.md:118` — "a hub touch raises Risk a level even when the edit itself is small"), which makes `security-review` mandatory (expected trivially PASS, prose only, but must be run and labelled); C1→C2 on the scope growth found here — three shipping files plus a pinned-test repoint with a documented wrong answer. Hard-Stop Gate 2's floor does **not** apply; none of its trigger words are present, so this is a judgment call the user may reverse | Stage 3 completed 2026-08-14 in a pre-made worktree `wt-t070` on branch `feat/t070-impl` (no `isolation`), forked at `2baecb9`. All three pointers repointed with per-file wording; `test_agent_guide_dedup.py:184` parametrize split so `CLAUDE.md` sits on a new `T070_BASELINE_REF` while `MANIFEST` keeps `8fc4dd2` — the entry was repointed, never deleted, so T066's cross-context-redundancy finding stays guarded. Stage 4: code-review **0 P0 / 0 P1 / 1 P2 / 0 P3** — the P2 was T070's own AC7 byte-pins written as standing invariants, the exact shape LR/`memory` records as "a scope guard committed as an invariant blocks what it guarded" (T065's `entries == 146`); fixed at `0d3053f` by scoping them to the review-time diff. security-review **PASS, 0 actionable**, Supervisor-run and labelled (12th occurrence of the built-in diffing the checked-out branch): the shipping diff is three markdown prose lines, no executable surface added. 9 mutation controls RED→GREEN, each confirmed landed via `git diff --stat` before its verdict and reverted with `cp`, never `git checkout`; one control was designed to **error** rather than free-pass when AC4's path list is typo'd — a direct answer to the negative-grep free-pass family. 11 new tests, **405 passed** + smoke PASS. **`verify` run by the user 2026-08-15, PASS** — driven at the *installer* surface rather than the suite: a fresh `setup.sh` into a throwaway repo shows all three pointers landing downstream fixed, and following them lands on 4 C0–C3 rows in each role guide vs **0** in `general-agent-template.md`. Two probes: the upgrade path (`update.sh` over a pre-T070 install) and a locally-customized template (conflict prompt held, local preserved, non-zero exit). **Two findings recorded, neither a blocker, neither folded in (Surgical Changes):** (a) the `CLAUDE.md` fix reaches **new installs only** — `CLAUDE.md` is absent from `MANIFEST` and `update.sh:276` names it as the example preserved-unprocessed lock entry, so existing downstream repos keep the stale line; `docs/claude-md/` **is** in MANIFEST so `pipeline-stages.md` propagates; (b) the literal phrase `Complexity matrix` occurs **0 times** in the role guides — the heading is `## Complexity & escalation` — so the noun the pointer teaches still greps to nothing. Same defect class as the one T070 fixes, one notch smaller | Completed: 2026-08-15
- [x] **T069** — T041's fix depends on a file that is not guaranteed to reach the agent. Surfaced during T066's Stage 4 review and deliberately **not** treated as a T066 regression: the compact Karpathy Engineering Principles table and the `Search Before You Build` ladder live **only** in `.claude/agents/general-agent-template.md`, both before and after T066 — verified by grep against the pre-task baseline, so T066 neither introduced nor worsened it. The problem is structural and was only made visible by T066's own framing: the harness auto-loads `.claude/agents/<name>.md` as an agent's system prompt, so a **role guide always arrives**, while the template arrives only if the agent chooses to open it (event trace: 7 `Read` records across 3 task buckets — a floor, not a rate, since per T063 the active-task pointer arms after the startup reads). So the two things T041 inlined *specifically because `CLAUDE.md` never reaches an agent* now sit one hop behind a read the agent may skip — the identical failure class, one level down, and the third instance of "already covered must mean reaches-the-context". Options to weigh at Stage 2, none obviously right: inline the compact table into all four role guides (guaranteed, but re-adds ~1,400 chars × 4 and partially reverses T066's saving); make the template read non-optional by asserting it in every role guide's startup sequence *and* the spawn prompt (cheap, but an instruction is not a guarantee — that is exactly what T065 disproved about element 4); or accept it and record why. **Do not start by assuming inlining wins** — T066 measured that the saving is real and c-infra's was already near-null | common-infrastructure | C2 | Risk: Medium | P1 | Registered 2026-08-09 from T066 Stage 4 | Stage 2 planned 2026-08-10; guide at `tasks/TASK_GUIDE_T069.md`. **The row listed three options and Stage 2 measurement produced a fourth, which the user selected: split by mandate.** The Karpathy table is a Permanent Rule — `CLAUDE.md` calls the principles "mandatory for the Supervisor and all sub-agents" — so it moves into all four role guides; the Search-Before-You-Build ladder is a T041 advisory checklist and **stays in the template**, pinned byte-identical by AC7. Measured: table 622 chars, ladder 1,045. Against T066's per-spawn *pair* savings (c-infra −480, backend −2,142, frontend −2,123, qa −2,165), inlining **both** costs c-infra **+1,187** — net worse than before T066 for the most-used role — while the table alone costs +142 and keeps ~71% of the saving. **The shipped design is cheaper than either**: because the table is removed from the template once all four guides carry it, the pair delta is +622 −622 = **0 net chars per spawn**, so the fix converts an optional read into a guaranteed one at zero measured cost (AC9 requires that measured and pasted, with a null result stated as null, not reframed). **Option B is already shipped and is the thing that is not working** — all four role guides already carry "4. Read `general-agent-template.md`" in their startup sequence, and the event trace over 66 buckets shows only **9 `Read` records on the template in 4 buckets** (T001×3, T065, T066, `_untagged`×4; a floor, not a rate, per T063); a second instruction would repeat exactly what T065 disproved about element 4. **Stage 2 pre-flight found the pinning test T064 warned about**: `scripts/test-agent-template.sh:45-60` asserts the four principle names and operational commands against `$TEMPLATE`, so no correct implementation satisfies both that assertion and AC2 — AC3 requires repointing it with the strings byte-identical plus a new assertion that the old location is empty, never loosening the `grep -qF`. Order is load-bearing (add to all four guides first, remove from the template second — the reverse strips a Permanent Rule from every spawn for as long as the intermediate state lives), which is T066's AC2 trap in a new form, and T066's own suite already encodes the governing invariant at `test_agent_guide_dedup.py:119`. Two stale Complexity-matrix pointers found and split out as **T070** rather than fixed here | Stage 3 completed 2026-08-10 in a pre-made worktree off the branch tip (no `isolation`), on its own branch `feat/t069-impl`. **A new worktree failure mode, recorded**: the first spawn was rejected by `pre_agent_validate_guide.py` with "missing TASK_GUIDE for T069" because the *worktree* held the task branch while the Supervisor's main checkout sat on `main` — the hook resolves the guide against the **main checkout**, not the agent's cwd, so it is not enough for the guide to be committed and visible to the agent. Fixed by keeping the main checkout on the task branch and giving the worktree its own branch. Order was honoured and is visible in history: `6473bea` ADD (table into all four guides, AC1 green while AC2/AC9 deliberately RED), `8b5bcb2` REMOVE — the table existed in **neither** location at no commit, and a test now enforces that as a tree property rather than trusting the history. The `common-infrastructure.md` insertion landed with the other three, not skipped. **Stage 4: 0 P0 / 0 P1 / 1 P2 (fixed) / 0 P3.** The P2 is instructive because it is T069's own defect class recurring inside T069's fix: all four role guides' startup step 4 still read "Read `general-agent-template.md` — Base Rules, the Karpathy Engineering Principles, and the Search-Before-You-Build ladder", a pointer naming a channel that no longer holds the content, sitting in the guaranteed channel itself. The guide's Files to Change predicted all four role guides but its AC table enumerated only the *template's* three prose statements — "retiring a convention touches more places than the AC table enumerates", repeating. Fixed at `284592f` after grepping the suite for a pin on the old wording (none found, per the T064 rule), and given its own assertion plus a mutation control at `f4d6f14`, because a Stage 4 fix ships untested unless someone writes one. **AC9 is a predicted-zero that did not hold, recorded as measured rather than reframed**: the guide predicted 0 net chars per spawn, the measured result is **+258 per role** (+210 before the P2 fix). The table move itself is exactly neutral (+622 into each guide, −622 out of the template, 3,762→3,350); the entire residual is prose that AC6 and the P2 fix required. The AC9 test asserts only `abs(delta) < len(table)` — that the move did not cost a *copy* — deliberately **not** `after <= before`, which would be T065's AC12 again, a scope guard frozen as an invariant that blocks the next legitimate template edit. **The `test-agent-template.sh` repoint is the T064 pinned-location case handled correctly**: the four principle names and four operational commands stay byte-identical with `grep -qF` unchanged, only the file each is checked against moved, plus a new AC1b negative proving the old location is really vacated and a file-existence guard so a mistyped path errors loudly instead of free-passing (the recorded negative-grep trap). **A new vacuous-assertion shape found by the implementer's own control**: `craft-agent/SKILL.md` states its rule twice — a drafting bullet and an emitted skeleton line — and each half free-passed an assertion aimed at both, so deleting either left the test green; the assertion was strengthened and both directions re-run RED. security-review **PASS, 0 actionable**, Supervisor-run and labelled (eleventh occurrence of the built-in diffing the checked-out branch — here it would have compared the task branch against `main` while the work sat on `feat/t069-impl`, a false PASS on a mandatory gate). No new executable ships; grep over added lines for `subprocess|os.system|eval(|exec(|urllib|requests|socket|chmod|rm -rf|curl|wget|shell=True` returns 0 hits; the shell script's only unquoted expansion is deliberate word-splitting over four space-free literals it defines itself, with every file argument quoted and no external input. AC8 independently checked: `AGENTS.md` unchanged and still accurate — it carries no Karpathy content at all, which T051 established deliberately. 8 implementer mutation controls plus the Supervisor's own, each confirmed applied via `git diff --stat` before its verdict and reverted with `cp` not `git checkout`. 21 new assertions, **394 passed** + smoke/validate/agent-template all exit 0, every exit code captured without a pipe. Two stale Complexity-matrix pointers found at Stage 2 and split out as **T070** rather than smuggled in | Completed: 2026-08-10
- [x] **T066** — de-duplicate the startup read set. `CLAUDE.md` (3,794 tok, installed as a real copy by `setup.sh:238`), `general-agent-template.md` (1,817) and the per-role agent guides (~730) all ship downstream and overlap. **Ranked last deliberately**: T039 already harvested most of this once, and the recorded learning that "already covered must mean reaches-the-context" says some overlap is deliberate redundancy across *different* context windows that buys reliability — collapsing it would be the exact error T041 was written to fix. Stage 2 must separate genuine duplication inside one context from redundancy across several. Note `PROJECT_SPEC.md` staleness is explicitly **out of scope** — it does not ship, so it is instance-only cleanup | common-infrastructure | C2 | Risk: Medium | P2 | Registered 2026-08-07. Gated on real T061 telemetry | Stage 2 planned 2026-08-09; guide at `tasks/TASK_GUIDE_T066.md`. **Stage 2 measured which overlap is which, and the answer inverts the obvious fix.** Two structural facts settle it. (1) **`CLAUDE.md` never reaches a sub-agent at all** — it is absent from the agent startup read list, so its 3,819 tok are Supervisor-context only and its overlap with the agent guides is *cross-context redundancy that must not be collapsed*; that is exactly T041's fix, which inlined a compact Karpathy table into `general-agent-template.md` **because** `CLAUDE.md` never arrives. AC5/AC6 pin `CLAUDE.md` byte-identical and pin the Karpathy table + Search-Before-You-Build ladder reachable from every role. (2) **The role guide is guaranteed in context; the template is not** — the harness auto-loads `.claude/agents/<name>.md` as the system prompt, while `general-agent-template.md` arrives only if the agent opens it (event trace: 7 `Read` records across 3 task buckets, and per T063 that is a **floor, not a rate**, since the active-task pointer arms after the startup reads). So consolidation goes *into* the guaranteed channel, not the tidy one: moving content out of a guaranteed channel into an optional one is the "already covered must mean reaches-the-context" error with extra steps. Measured duplication for sections carried by both: Mandatory Startup 1,019 (template) vs 242/453/453/433; Communication Protocol 338 vs **0**/338/338/399; Available Skills 973 vs 481/1,137/1,038/1,034; Complexity 1,689 vs **0**/1,435/1,340/1,333 — 10,454 chars (~2,613 tok) restated, against a per-spawn pair cost of 2,541 tok (c-infra) to 3,482 tok (backend). **AC2 is the trap and it protects the most-used agent type**: `common-infrastructure.md` has *neither* Communication Protocol nor Complexity today, relying entirely on the template, so the first deletion from the template would silently strip both from every c-infra spawn — they must be folded in first. AC7 requires the per-role before/after token table to be **measured and pasted**, with a null result stated rather than reframed. `PROJECT_SPEC.md` staleness stays out of scope per the row (it does not ship) | common-infrastructure | C2 | Risk: Medium | P2 | Registered 2026-08-07. Gate cleared: real T061 telemetry landed; T065 merged so what reaches an agent is now documented accurately | Stage 3 completed 2026-08-09 in a pre-made worktree off the branch tip (no `isolation`). Consolidation ran **into the role guides** per the guide's counter-intuitive direction: all four gained the full startup sequence and an inlined report format, and `common-infrastructure.md` — the AC2 trap and the most-used role — gained `## Complexity & escalation` and `## Communication Protocol`, of which it had **zero** chars. `general-agent-template.md` dropped the four fully-shared sections (7,246 → 3,762 chars), keeping Base Rules, the Karpathy table, the Search-Before-You-Build ladder, Output Requirements and the Staleness Guard. `craft-agent/SKILL.md` updated so newly generated role guides are not born non-compliant (the guide's edge case #6). AC7 measured and independently reproduced by the Supervisor in characters: c-infra 10,167→9,687 (**−4.7%**), backend 13,928→11,786 (−15.4%), frontend 13,581→11,458 (−15.6%), qa 12,748→10,583 (−17.0%). **The agent reported c-infra as near-null rather than averaging it away** — it is the one role that was *missing* two sections, so it buys new content out of the same saving the others bank outright. Stage 4: 0 P0 / 0 P1 / 0 P2. **Two defects were self-caught by the implementer and both are instructive.** First, its own AC7 test passed *while the files were still untouched*: `git show` returns bytes and these guides are dense with `—`/`≤`, so a byte baseline against a `read_text` char current manufactured a ~4% saving — a new vacuous-assertion shape whose tell is a comparison whose two sides came from different readers. Second, `pytest -q 2>&1 | tail -2 && git commit` committed a red suite, because `tail` always exits 0 and `&&` gates on the *last* command of a pipeline; the Supervisor re-checked the real exit code without a pipe (`exit=0`, `373 passed`). It also escalated that T065's `test_memory_channel_and_budget.py:199` pins a string inside the very section AC3 removes (the T064 "a test pins a section's location" family) and resolved it **without touching the test** — the sentence survives byte-identical as a Base Rule the template legitimately keeps — and that filling its own Evidence `verify` row while Result still read `☐ pass` was rejected by T068's new gate, which it fixed in its file rather than in the gate. **T068 working as designed on its first live encounter.** security-review **PASS, 0 actionable**, Supervisor-run (tenth occurrence of the built-in diffing the checked-out branch): the one new executable, `scripts/measure_agent_guide_tokens.py`, calls `subprocess.run` with an argument list and never `shell=True`, is read-only, and has no network or `eval` surface. 3 mutation controls RED→GREEN, each confirmed applied first, with AC6 attacked from two directions (delete the whole Karpathy table; delete only its heading). AC5/AC10 verified by empty diff. **One Supervisor error, caught and fixed**: the security-review block was inserted mid-table and its H3 heading truncated the Evidence section, orphaning the three UI rows outside it — moved below the table and re-verified through the real resolver. 35 new tests, **373 passed** + smoke PASS. **Finding registered as T069, not a T066 regression**: the Karpathy table and the ladder live **only** in the template both before and after, so T041's fix depends on agents opening a file that is not guaranteed to be opened — the same failure class T041 was written to fix, one level down | Completed: 2026-08-09
- [x] **T065** — make the MEMORY.md size gate measure what it costs. The 200-line cap is enforced by line count while cost is chars: the file's own stated format is `≤150 chars` per index entry, and on 2026-08-07 126 of 147 entries (86%) violated it, mean 281, max 710. The Supervisor hit that cap three times in one session and trimmed to satisfy it while writing replacement entries longer than the ones removed — the gate passed every time. **The content is instance-only and does not ship** (`setup.sh:344` seeds a fresh stub), but the *mechanism* does: the cap test lives in `.claude/hooks/tests` and the Memory Write Protocol that produced 281-char "one-line summaries" lives in `docs/claude-md/`. So the value is future-proofing every downstream repo, not tokens saved here today. Depends on T063 — if memory is not used, deletion beats compression | common-infrastructure | C1 | Risk: Medium | P2 | Registered 2026-08-07. Depends on T063 | Stage 2 planned 2026-08-09; guide at `tasks/TASK_GUIDE_T065.md`. **Raised C1 → C2**: registered as one test assertion, but the documented contract is restated in eight shipping locations and `setup.sh` seeds it into every downstream repo. **Channel ruling by the user 2026-08-09**: amend `craft-spawn-prompt` element 4 to match observed practice (pass the path, agent reads it) rather than making practice match the mandate — T063 found 0 of 49 spawn prompts carried the file's H1 and 5 of 5 agents opened it themselves, and both spawns this session did the same, making three independent confirmations. **The sharp end is `docs/claude-md/pipeline-stages.md:155`**, which tells agents "the agent must not re-read it; it is already in context" — under the real channel that sentence is false and an agent obeying it never reads memory at all, so AC6 pins its removal. Gate evidence measured at Stage 2 across the last 12 commits touching the file: lines pinned at 200–201 while chars grew 42,577 → 49,156 (**+15.5%**) and mean entry 276 → 326, **green on every commit**; 130 of 146 entries (89%) now exceed the documented `≤150 chars`, mean 326, max 796 — worse than the 86%/281/710 recorded when the row was written, and twice this session the Supervisor consolidated entries to satisfy the line cap while adding longer replacements, so line count fell as chars rose. New gate is a 52,000-char budget (current + ~6% headroom) **explicitly documented as a ratchet — lowerable by `/compact-memory`, never raisable** — plus advisory per-entry reporting that must not fail the suite, since a hard per-entry cap would turn it red on arrival. Content compaction is explicitly out of scope (AC12 pins the index entries unmodified); this task ships the mechanism, which is what reaches downstream, not the instance-only content | common-infrastructure | C2 | Risk: Medium | P2 | Registered 2026-08-07. Depends on T063 — resolved, T063 merged | Stage 3 completed 2026-08-09 in a pre-made worktree off the branch tip (no `isolation`). **The guide predicted eight shipping locations; the sweep found eleven.** Three more contract statements and, critically, a **second copy of the harmful sentence** in `.claude/agents/general-agent-template.md` — "the Supervisor pastes `memory/MEMORY.md` into your spawn prompt; do not re-read it" — which is the copy sub-agents actually obey, and which the guide's AC table did not name; `compact-memory/SKILL.md` had a second spot found only by running the AC9 negative test. Textbook instance of the recorded "retiring a convention touches more places than the AC table enumerates", and the file-wide negative grep found 3 of the 11. Gate is now `HOT_TIER_CHAR_BUDGET = 52_000` with the measurement helpers extracted so the controls can drive the real gate on a copy; the budget ships with an in-code comment naming it a **ratchet** that `/compact-memory` may lower and nothing may raise, which is the part that makes the fix stick — the number alone would decay exactly as the line cap did. Per-entry 150 chars is now explicitly **advisory and reported, never enforced** (89% violate it; a hard gate would have turned the suite red on arrival). AC12 held exactly: `memory/MEMORY.md` diff is 6 insertions / 3 deletions, all header rules, **zero index entries touched**, still 146 entries. Stage 4: 0 P0 / 0 P1 / 0 P2, one escalation signed off. **The agent escalated rather than guessing on four points, and was right on all four** — most notably that it could not run `security-review`/`code-review`/`verify` at all, since `Skill()` is not in a sub-agent's toolset, and it labelled them manual instead of claiming a skill run it had not performed. **security-review therefore run by the Supervisor: PASS, 0 actionable**, manual and labelled (ninth occurrence of the built-in diffing the checked-out branch). Every change is prose except one test assertion; both hook edits are comment-text only; `grep` for `subprocess|os.system|eval|exec|urllib|requests|socket|chmod|rm -rf` over added lines returns 0. The one real surface is `setup.sh`'s seeded stub, whose new text introduces backticks into a heredoc — **verified empirically by running `scaffold_project` into a temp dir** and confirming they render literally, since the heredoc is still `<<'EOF'`. Escalation on a stale present-tense docstring in `test_move_to_review.py` signed off and fixed: the agent's "don't rewrite fixture prose to green a sibling assertion" reasoning is right for fixtures but this was a docstring asserting a retired contract, and the identical sentence in the hook itself had already been past-tensed. 5 implementer mutation controls RED→GREEN, each confirmed landed before its verdict, **plus the Supervisor's own independent control from a different construction**: +400 chars on each of 10 entries → `49,451 → 53,461` chars with a line delta of exactly **0** → new gate RED, old gate structurally blind. 12 new tests, **338 passed** + smoke PASS. **Flagged, not a defect**: the live file sits at 49,451 / 52,000 (95.1%), roughly 7 average entries of headroom, so the next memory pass will likely trip the gate — that is the gate working, and the sanctioned response is `/compact-memory`, never editing the budget. Also left deliberately: the memory gate still lives in `test_token_audit_format.py`, a file it has nothing to do with, and T065 made that file half memory code — worth a follow-up | Completed: 2026-08-09
- [x] **T068** — an *unfilled* `verify` Evidence row already satisfies the merge gate. Found by the T064 implementer during Stage 3 and confirmed pre-existing by the Supervisor against baseline `2612a05`, so it is not a T064 regression. `pre_bash_block_unsafe_merge.py` matches `verify\s*\|[^|\n]+\|[^|\n]*pass`, requiring the word "pass" in the *Notes* column — which is exactly the T026 two-bug fix and correct in intent. The defect is that T026 also wrote the word "pass" into the template's own placeholder **as guidance** (`must literally state "pass" or "fail" here too, e.g. "skill run, feature confirmed working — pass"`), so a task that has filled in nothing at all already clears the row check; only `trace_shows_verification` still stands between it and a merge. This is the **fourth distinct way this gate has failed to gate** (after the T044 substring match, the `extract_command` description leak, and the false-PASS-from-the-wrong-branch security-review pattern) and the third defect whose root cause is guidance text being indistinguishable from filled-in evidence. T064 makes it more visible, not worse: every new task now starts from a review file carrying that placeholder. Fix direction to weigh at Stage 2 — reword the placeholder so it cannot match (but a test pins that string, and the recorded rule is to fix the prose around a pinned string, not loosen the test); require a checked `☑` in the Result column; or match on the *absence* of the unchecked `☐` glyphs. Must not weaken the Notes-column requirement itself, which is the property T026 established | common-infrastructure | C1 | Risk: Medium | P1 | Registered 2026-08-09 from T064 Stage 3 escalation 2 | Stage 2 planned 2026-08-09; guide at `tasks/TASK_GUIDE_T068.md`. Stage 2 surveyed every real `verify` row in `tasks/`, `templates/` and the bugfix skeleton and pinned the corpus into the guide as the oracle: unfilled shapes are `☐ pass / ☐ fail / ☐ N/A` (3) and `☐ pass / ☐ fail` (2); filled shapes are `☑ pass` (~25), bare `pass` (4), `✅ pass` (1) and — the trap — T050's `☑ pass / ☐ N/A`, which is legitimately filled yet still contains `☐`. **So the obvious fix, rejecting any Result cell containing `☐`, would block honest historical work and is forbidden by AC3.** Recommended rule is strictly narrower: reject only the literal `☐ pass`, which classifies all six observed shapes correctly. AC8 forbids fixing this by rewording the template placeholder — a test pins that string, the wording is genuinely useful reviewer guidance, and a matcher that only works because nobody wrote "pass" in a comment is the same defect class as the T044 substring match. AC6 requires running old and new patterns over the whole real corpus and diffing verdicts before the fix is trusted | Stage 3 completed 2026-08-09 in a pre-made worktree off the branch tip (no `isolation`). Rule chosen is the guide's recommended narrow one: `VERIFY_ROW_PATTERN` now captures Result and Notes as named groups, keeping T026's three-part shape, and a new `UNCHECKED_PASS_PATTERN = r"☐\s*pass\b"` disqualifies a row only when its **Result** cell carries a literal unchecked *pass*. All six corpus shapes classify correctly, including the `☑ pass / ☐ N/A` trap that the obvious "reject any ☐" rule would have broken. **Stage 4: 0 P0 / 1 P1 fixed / 0 P2 / 1 P3 fixed, both found by the Supervisor.** The P1 is the significant one and is the 7th entry in the vacuous-assertion family: the new test file's `row_is_filled` helper **re-implemented the gate's decision** — walk `VERIFY_ROW_PATTERN`, apply `UNCHECKED_PASS_PATTERN` — instead of calling the shipped `has_filled_verify_row`, and degraded defensively through `getattr(..., None)`, so all 8 new assertions exercised a copy of the logic living in the test file. Proven vacuous by mutation: making the **real** function ignore `UNCHECKED_PASS_PATTERN`, which restores the exact defect this task exists to fix, left the suite at `326 passed` catching nothing. The helper now writes a real `TASK_REVIEW` file to a temp dir and calls the real function, and the identical mutation turns AC1 RED (`1 failed, 325 passed`). **The P3 is a false provenance claim that originated in the Supervisor's own Stage 2 guide**: the AC table attributed the `☑ pass / ☐ N/A` shape to T050, but T050's real row is `☑ pass` with no unchecked glyph and the shape actually occurs in `TASK_GUIDE_T063.md`; both real rows omit "pass" from Notes and are correctly rejected by old and new patterns alike, so the fixture is genuinely synthetic and now says so rather than claiming a provenance it does not have (the recorded T061 failure, repeating). security-review **PASS, 0 actionable**, run manually and labelled (ninth occurrence of the built-in diffing the checked-out branch): the production diff is regex-only, adds no input surface, no writes and no network, and both patterns are linear with no nested quantifiers. Verified end-to-end against real files, not fixtures — an untouched `templates/TASK_REVIEW_template.md` now yields `False`, which is exactly the case that used to clear the gate. Also confirmed the self-referential trap did **not** fire: T068's own review file quotes `☐ pass` while documenting the defect and still resolves `True` from its genuinely-filled row. Recorded honestly: `T050` still returns `False`, unchanged from the old pattern, because its Notes cell omits "pass" — the T026 property, not a regression. 9 new tests, **326 passed** + smoke PASS | Completed: 2026-08-09
- [x] **T064** — split the reviewer-filled sections out of the implementer's guide. Registered as "prose to challenge-response cards"; `templates/TASK_GUIDE_template.md` ships to every downstream repo, so guide shape is one of the few per-task variables the harness actually controls, and T060's guide ran 6,836 tokens, larger than CLAUDE.md itself. **The registered direction was rejected at Stage 2 on 2026-08-09 by user ruling, not deferred**, and the scope replaced. Two things forced it. First, T063 established the guide is not injected verbatim — the agent opens it with `Read`, so its bytes are cache_read re-paid every agent turn, and the real T061 telemetry this task was gated on now shows `cache_creation` flat at **265–423 tokens** across a bare-`echo` probe (15,727 total), T067 (83,802) and T063 (111,056) regardless of guide size; a 6,800-token guide never appears there, confirming the row's own "~10% of nominal" demotion. Second, measuring the two most recent guides by section showed the largest block is not reasoning prose but reviewer-filled scaffolding the implementer never uses: `## Demonstration` (5,443 ch on T060, 4,633 on T067) plus the `### Evidence` table (~3,000 ch) are filled at Stage 4/5, ~35% of guide bytes, against `## Approach` at 5,693/3,026 and `## Requirement` at 2,135/2,543 which the implementer does use. So the new scope moves those two sections to a sibling `tasks/TASK_REVIEW_Txxx.md` and leaves every byte of reasoning prose exactly where it is — which also **dissolves the load-bearing risk the row was registered with** (that a card format stripping recorded risks, rejected directions and prior art would delete the very thing that made T058 and T060 land correctly); AC11 now pins the card format as a file-wide negative so the rejected direction cannot re-enter through the implementation. Ships as **fallback, not migration**: every consumer reads the guide first and falls back to the sibling file, so all 20 existing guides keep both sections inline and are asserted byte-identical (AC14) — deliberate, because this hook family has five recorded parsing defects (T018/T022/T024/T042/T045) and a big-bang migration would move all 20 onto the new path at once. Three consumers resolve through one new shared helper rather than three copies of the same fallback. **AC7 is the load-bearing criterion**: the merge gate must still block when a task has neither an inline Evidence table nor a review file — treating "file missing" as anything but "no evidence" retires the gate silently, on every task, so that test is written first. MANIFEST deliberately unchanged (line 11 is the bare `templates` directory entry copied with `cp -r`, the same call recorded on T054). Explicitly out of scope: template boilerplate trimming, startup-read dedup and MEMORY.md shrinking (T066/T065), and any claim this recovers the ~15.7k spawn floor — DDR-0004 already ruled it does not | common-infrastructure | C2 | Risk: Medium | P2 | Registered 2026-08-07. Gate cleared: real T061 telemetry landed via T067/T063 | Stage 2 planned 2026-08-09; guide at `tasks/TASK_GUIDE_T064.md` | Stage 3 completed 2026-08-09 in a pre-made worktree off the branch tip (no `isolation`). One new `.claude/hooks/lib/guide_sections.py` resolver serves all three consumers; `templates/TASK_REVIEW_template.md` added; both sections vacated from the guide template with a `> **Moved.**` pointer left in place; bugfix skeleton split the same way so the two flavors keep identical field names (the property `delivery-report` relies on to need no flavor branch). AC15 measured honestly across five real guides — T060 33.6%, T061 32.3%, T063 31.4%, T067 30.5%, **T058 16.3%** — so the claim recorded is "~31–34% typical, not a floor", with T058 an outlier on an unusually large `## Approach`; a control of one-line sections measures −0.4%, so the metric discriminates. **The agent escalated two pre-existing tests instead of editing them green, and the escalation was correct**: `test_fixed_row_present_in_template_file` and `test_ac7_gate_finds_verify_row_in_real_skeleton_shaped_guide` both pin the `verify` row's **file**, not its shape, and the file is exactly what this task vacates — unsatisfiable alongside AC2/AC12. Signed off and repointed by the Supervisor with the row string byte-identical, plus a new assertion that the guide template does **not** carry it so the move cannot silently revert; T055's property (the bugfix flavor produces a gate-visible verify row) re-verified at the new location. Stage 4: code-review 0 P0 / 0 P1 / **1 P2 fixed** / 0 P3. **The P2 is the instructive one and the Supervisor found it, not the implementer**: the resolver was imported unguarded with a comment claiming that made the gate fail closed, but this hook signals a block by printing `decision: block` on stdout and exiting 0 — a bare `ImportError` exits 1 with *empty* stdout, which the harness reads as a non-blocking hook error and the merge proceeds. Reproduced empirically (`exit=1`, `stdout=[]`), so the one place AC7 designates fail-closed was the one place failing open. Import now guarded and its failure emits an explicit block; pinned by a subprocess test, since the failure exists only at module-import time and an in-process import cannot exercise it, and mutation-verified RED→GREEN — the recorded "a review fix can be as unpinned as the defect it fixes" applied to the fix itself. security-review **PASS, 0 actionable**, run manually and labelled (eighth occurrence of the built-in diffing the checked-out branch, which was not `t064-work`): the only new surface is a read path built from `task_id`, and all 10 hostile probes (`../../../etc/passwd`, `T064/../../..`, `$(whoami)`, NUL, 200-char, newline-injection) were rejected by an anchored digits-only allowlist. 4 implementer mutation controls RED→GREEN plus 2 Supervisor controls; **the Supervisor's first control did not apply and its own assert caught it** — a live demonstration of the recorded "a mutation that does not take effect proves nothing", after which the real one turned 11 tests RED. 50 new tests, **317 passed** + smoke PASS. **Escalation 2, deliberately not fixed here**: the template's *unfilled* `verify` row already satisfies the merge gate, because T026 put the word "pass" into the Notes column as guidance and the gate matches that column — reproduced at baseline `2612a05`, so it is pre-existing and out of scope, registered as **T068** | Completed: 2026-08-09
- [x] **T063** — establish whether injected memory is actually used, before T065 decides to compress it. `memory/MEMORY.md` is ~10,700 tokens and `CLAUDE.md` says it is injected in full into every spawn, but nothing has established that a sub-agent draws on it. **A first look on 2026-08-07 produced a number that cannot be trusted, and that is the task's starting point**: counting `Read` records in `event-trace` showed only 5 of 49 tasks with any read of `MEMORY.md`, but the actor is unknown — T067's trace holds bash 12 / edit 13 / read 1 / write 2 while its agent self-reported bash 14 / edit 15 / read 4, close but not equal, so the trace may be recording the sub-agent's calls, the Supervisor's own, or a mix. The 5-of-49 figure may simply show that the *Supervisor* rarely re-reads memory. Building on that instrument without first establishing what it measures is exactly the failure that retired DDR-0001 and DDR-0002, so attribution is AC1, not a footnote. A second confound must also be separated: spawn prompts this session restated memory content inline (T059's and T067's both warned that `git checkout` is guardrail-blocked, itself a recorded memory learning), so an agent obeying it proves nothing about `MEMORY.md`. Work order is deliberate — settle attribution, then whether memory reaches an agent verbatim or as a path it may never open, and only then attempt a usage signal. **The most likely honest result is "not established", and the guide states that is a successful outcome, not a failure**: AC6 forbids concluding memory is unused from an absence of citations. Deliverable is a read-only script plus a written finding ending in a stated implication for T065 — compress, delete, shrink, or cannot yet decide. AC9 (the script writes nothing) is load-bearing and must be mutation-verified from two directions, since T059 was a defect of exactly this shape and the vacuous-assertion family now has six entries | common-infrastructure | C1 | Risk: Low | P2 | Stage 2 planned 2026-08-07. Gates T065 | Finding lives in `docs/`, not `reports/` (Supervisor call 2026-08-07 — `reports/` is gitignored except `token-audit_*.md`, so it would die in the worktree per the T028 gotcha; this is durable analysis, not regenerable output). Stage 3 spawned 2026-08-07 | Stage 3 completed 2026-08-07. **(a) ESTABLISHED**: `event-trace` records both the Supervisor's and the sub-agent's calls in one file, separable by time window but not by bucket — proven two ways, first-person (T063's own records) and by reconciling T067's `spawn.tool_stats` against windowed buckets. **The naive 5-of-49 figure measures when agents arm the active-task pointer, not whether they read memory**: element 6 arms it "before running any test or verification command", which is after the mandatory startup reads, so those land in `_untagged` — 30 `MEMORY.md` reads there against 6 across 61 task buckets. **(b) ESTABLISHED for the current template**: `craft-spawn-prompt` element 4 mandates verbatim injection; practice passes a **path**. Zero of 49 `Agent` records contain `MEMORY.md`'s H1, 4 name the path, and 5 of 5 recent agents opened the file — pointless if handed the contents. Hedged correctly: 48 of 49 summaries sit at the 300-char `MAX_SUMMARY_LEN`, so a later paste cannot be ruled out. **(c) NOT ESTABLISHED**, and AC6 held — `post_tool_trace.py` never stores agent output, so no citation corpus exists in principle; the report shows *opened*, not *drawn upon*, and refuses to infer non-use from absent evidence. **This invalidated the Supervisor's own earlier premise**: the claim that `MEMORY.md` costs ~10,700 tokens injected into every spawn is false — it is passed as a path costing ~20 tokens. **(d)** therefore reframes T065: it is a *channel* decision first (honour element 4 or amend it to match the path practice), then shrink the hot tier; do not compress on the old premise, and do not delete without evidence of non-use. **Two self-corrections by the implementer are the strongest part of the work**: its first reconciliation produced a perfect `33 == 33` from two errors cancelling (an unwindowed bucket still holding the Supervisor's pre-spawn guide Write compensated for a missing agent edit) — published numbers are the windowed ones with a 1-call residual flagged as unexplained; and its own AC9 mutation initially passed GREEN, fixed with an import-time git-status baseline rather than accepted. Three instrument defects reported and deliberately not fixed (pointer armed too late, no actor field in the trace, 300-char summary cap). Stage 4: 0 P0 / 0 P1 / 0 P2 / **1 P3 fixed** — §d.3's "~500x cheaper per spawn" overstated a prompt-side saving as a spawn-side one, when §b found 5 of 5 agents open the file anyway; clarified so T065 is scoped on cost becoming *optional* rather than eliminated. security-review not required (Low risk), stated rather than left blank. **A Supervisor mutation control initially stayed GREEN and looked like a real gap, but was inert** — the write was appended after `sys.exit(main())` and never executed; re-done inside `main()` it turned both guards RED. 9 new tests, **267 passed** | Completed: 2026-08-07
- [x] **T062** — spawn threshold — stop paying the ~15.7k per-spawn floor for C0/C1 work. **Promoted from last to first on evidence**: probe arms on 2026-08-07 showed every spawn costs ~15,669 tokens before doing any work (a single `echo`), against 48,401 for T059's real three-line fix and 81,220 for T060 — so eliminating a spawn saves the entire floor, while trimming injected artifacts nibbles at ~10% of the variable part (nearly everything injected is billed as a cache read). Proposal: below a size threshold the Supervisor does the work inline instead of spawning. **BLOCKED on a user decision, not on engineering**: this collides directly with Hard-Stop Gate 1, a Permanent Rule stating the Supervisor must never write implementation code. Options to weigh at Stage 2 — carve a C0/C1 exemption into the Gate; keep the Gate and instead make spawns cheaper; or reject the direction and accept the floor as the cost of isolation. Must not be started until that ruling is made, and the ruling belongs to the user. Also gated on real T061 data: the numbers above are n=3 synthetic spawns, recorded as directional, not established | common-infrastructure | C2 | Risk: High | P1 | Registered 2026-08-07 from the ideation session in BRAINSTORMING_LOG.md. **Blocked**: needs a user ruling on Hard-Stop Gate 1 + real T061 telemetry | **CLOSED WITHOUT IMPLEMENTATION 2026-08-07 by user ruling — see DDR-0004.** Hard-Stop Gate 1 stands unchanged; the harness pursues *cheaper* spawns (T064/T065/T066) rather than *fewer* spawns, accepting the ~15.7k floor as the standing price of sub-agent isolation. Alternatives weighed: a C0/C1 exemption (largest saving, rejected — LR-0001/LR-0002 both record the Supervisor drifting into implementation because a task "felt small", which an exemption would legitimise by name, and it collapses implementer and reviewer into one role, a separation that on 2026-08-07 alone caught a false fixture provenance claim, a vacuous line-cap assertion and three stale Evidence tables); outright rejection of any spawn-cost work (needlessly absolute); and deferral (correct for sequencing T064/T066, but more cost data cannot answer whether a Permanent Rule should bend — that is a judgment about review independence, not tokens). **Accepted consequence, stated not hidden**: the largest measured lever is deliberately not pulled, and the chosen path has a low ceiling — ~97% of injected context arrives as cache reads, so trimming it recovers roughly a tenth of nominal. T064/T065/T066 will not recover the floor and should not be expected to. Weak counter-evidence recorded rather than omitted: the step-limit hook killed the T053 and T055 agents mid-task and the Supervisor completed both itself, including all 8 of T053's tests, both passing Stage 4 and merging clean — n=2, unplanned, and the Supervisor was also the reviewer | Closed: 2026-08-07
- [x] **T067** — `diagnose` gains a root-cause rule, backward call-chain tracing, and behaviour-triggered red flags. From a prior-art comparison against `github.com/obra/superpowers/tree/main/skills/systematic-debugging` (SKILL.md + root-cause-tracing.md), consulted 2026-08-07 at the user's pointer. The sharpest gap, verified against the live file at `d374cb1`: **the string "root cause" occurs exactly once in all 124 lines, in the Communication Protocol notification template on line 124** — the skill asks the agent to *report* a root cause but nowhere requires the fix to be *at* one. Phase 5 constrains when the fix is written, never where it lands; Phase 6's "what would have prevented this?" is post-hoc prevention, not a constraint. Six additions, each placed where the agent is already looking: the root-cause rule in the Karpathy block and restated in Phase 5 at the point of action; five-step backward tracing in Phase 4 alongside Placement, scoped to the value-wrong-on-arrival shape; a red-flag block immediately before the Stuck-Loop Checkpoint as its earlier-firing sibling (the Checkpoint counts disproven hypotheses after two cycles have burned, red flags catch the behaviour first); working-reference comparison in Phase 3 as a hypothesis source; stack capture and test-visible probe output in the Phase 4 payload; and the architectural tell — each fix revealing new coupling elsewhere — added to the Checkpoint's widen-scope option. **Defense-in-depth REJECTED, not deferred** (AC10 pins it as a file-wide negative, the same device T058 used for its rejected transport layer): its own example adds four redundant guards for one bug, which contradicts Simplicity First and Surgical Changes, and Phase 5 already reverts REJECTED-hypothesis changes precisely to stop guards accumulating. Deliberately not replaced, because ours is stronger: their one-line "reproduce reliably" vs our ten-rung ladder; their single hypothesis vs our 3–5 ranked falsifiable; their ad-hoc `console.error` vs our NDJSON with mandatory `hypothesisId`, 1–10 budget, CONFIRMED/REJECTED/INCONCLUSIVE verdicts and T060's cross-tier correlation; their 3-failed-fix-attempts escalation vs T052's earlier 2-consecutive-disproof trigger. **Hard line-count cap of 165** (124 today, ≤41 net): if the six additions do not fit, tighten the prose rather than raise the cap — a skill nobody finishes reading enforces nothing. AC10 and AC15 both pass trivially and must be mutation-verified by *introducing* the forbidden token and *padding past* the cap, per the five recorded vacuous-assertion incidents | common-infrastructure | C2 | Risk: Medium | P1 | Stage 2 planned 2026-08-07 | Stage 3 spawned 2026-08-07. Highest-priority **executable** task — T062 is P1 but blocked on a user ruling | Stage 3 completed 2026-08-07 in a pre-made worktree off the branch tip (no `isolation`). All six additions landed at 155 lines, 10 under the cap; none dropped, cap not raised. **The agent escalated a genuine AC conflict rather than papering over it, and it was the Supervisor's error**: AC7 mandates changing Phase 3 while AC16 said all pre-existing tests pass unmodified — but `test_sc8_phase_1_2_3_bodies_are_byte_identical` pins Phase 3's body, so the two cannot both hold. It re-captured the hash rather than deleting the pin (min-len also raised 150→600, so Phase 3 is now pinned harder than before) and flagged it for sign-off. Signed off: deleting the pin would have lost coverage, leaving it would have made AC7 impossible. The agent also caught that AC16's stated count was wrong — 32 pre-existing tests, not 31. Both are instances of the recorded T054 learning that an AC can be written against a file's older shape, repeating because the guide was written without checking what the existing tests actually pinned. Stage 4: code-review 0 P0 / 0 P1 / 0 P2 / **1 P3 fixed**. The P3 is instructive: the AC15 line-cap assertion used `rstrip` on newlines, stripping *all* trailing ones, so blank-line padding past the cap was invisible while the docstring claimed the whole file was counted — 12 appended blank lines took the file to 167 by line count and the suite stayed green. **The implementer's own control padded with non-blank lines and passed; the Supervisor's padded with blank lines and did not.** An assertion can be non-vacuous against one mutation and vacuous against another, which is precisely why the implementer must not be its own sole oracle. security-review **PASS, 0 actionable**, run manually and labelled (seventh occurrence of the built-in diffing the checked-out branch) — instruction text only, no executable surface. 18 implementer mutation controls RED→GREEN plus 3 Supervisor controls. Defense-in-depth confirmed absent under both spellings; Phase 1/2 byte-identical against `d374cb1` (1052/157 chars); heading list identical; budget ceiling 10, 2-consecutive-REJECTED and 3–5 hypotheses all intact. 12 new tests, **258 passed**. **Also the first real production reading from T061's telemetry**: this task's own spawn was captured automatically — 83,802 total, 81,781 cache_read (97.6%), 265 cache_creation, 1,754 output, 33 tool uses, +464/−31 lines, opus-5, 498s — corroborating the n=3 synthetic finding on a real C2 task | Completed: 2026-08-07
- [x] **T061** — capture the per-spawn cost telemetry the harness already hands the hook. `post_tool_trace.py:53` reads `tool_response` out of every PostToolUse event and uses exactly one field from it, `is_error`; the record it writes is `summarize(tool_input)`, the prompt going in, so everything the spawn reports coming back is discarded. Measured empirically 2026-08-07 with a temporary probe registered on PostToolUse/Agent (three spawns, then reverted): the payload carries `totalTokens`, `totalToolUseCount`, `totalDurationMs`, `resolvedModel`, `agentType`, `status`, a full `usage` breakdown (input/output/cache_creation/cache_read) and `toolStats` (read/search/bash/editFile counts plus linesAdded/linesRemoved). 42 Agent spawns already in `memory/event-trace/` threw all of it away. Scoped to capture only — no analysis, no dashboard, no optimization, and explicitly no `/cost` or session-level accounting, which is what DDR-0002 retired. **This does not reverse DDR-0002**: that ruling governed a manual channel that failed twice because it depended on a human pasting a number; this one depends on no human action and cannot fail the same way. Two hypotheses were tested during investigation and **one was rejected**, which is why the scope is capture-only: H1 (the payload carries structured cost fields) CONFIRMED; **H2 (unique per-task content is paid at cache_creation rates while stable content is cache_read) REJECTED** — arm B added 1,115 tokens of novel prompt text and moved cache_creation by −1, because the spawn prompt is already inside the Supervisor's cached context before the agent starts. Consequence: token-volume optimization is worth roughly a tenth of its nominal figure, and the larger lever is spawn *count* — every spawn costs ~15.7k tokens before doing any work (15,669 for a single `echo`) against 48,401 for T059's real three-line fix and 81,220 for T060. **That rests on n=3 synthetic spawns and must not be treated as established** — it is the reason this task ships measurement rather than any optimization | common-infrastructure | C2 | Risk: Medium | P1 | Stage 2 planned 2026-08-07 | Stage 3 spawned 2026-08-07 from the ideation session in BRAINSTORMING_LOG.md | Stage 3 completed 2026-08-07 in a pre-made worktree off the branch tip (no `isolation`). `post_tool_trace.py` gains a guarded `extract_spawn` writing a `spawn` object for `Agent` records only — `total_tokens`, `tool_use_count`, `duration_ms`, `resolved_model`, `agent_type`, `status`, a `usage` sub-object split by cache disposition, and a `tool_stats` sub-object incl. `lines_added`/`lines_removed`; harness camelCase translated to snake_case on the way in; `prompt`, `content` and the unbounded `usage.iterations` deliberately not copied. Stage 4: code-review 0 P0 / 0 P1 / **1 P2 fixed** / 2 P3 accepted. The P2 was the dropped `cache_creation` TTL split — the two tiers are priced differently, so for a cost-telemetry task its absence forces any analysis to assume all creation is 5m; now flattened to `cache_creation_5m`/`cache_creation_1h`. **Writing the assertion for that fix exposed a second defect**: the fixture claimed to be pinned to the real 2026-08-07 probe capture but its nested keys were invented (`ephemeral_5m` vs the real `ephemeral_5m_input_tokens`), so a field written against the fixture would have silently never populated — corrected, with a control confirming that reverting to the invented name turns the test RED. The AC2 exact-equality assertion was updated to the new intended shape rather than loosened to a subset match. security-review **PASS, 0 actionable**, run manually and labelled (sixth occurrence of the built-in diffing the checked-out branch); the change only reads fields already handed to the hook and writes to a path it already wrote to. **The agent's one acknowledged gap was closed rather than accepted** — it correctly reported it could not produce a real `tool_response`, so the Supervisor drove the new hook with the two verbatim payloads captured by the probe, and both reproduce their hand-measured numbers; AC4 additionally re-verified by differential against the pre-change hook at `82883a2`, byte-identical. 8 mutation controls RED→GREEN (5 implementer, 3 Supervisor). 26 new tests, **246 passed** | Completed: 2026-08-07
- [x] **T060** — diagnose — cross-tier boundary instrumentation and correlated trace reporting. T058 turned Phase 4 into an evidence-driven loop, but its Placement step is a flat menu of seam types (function entry, function exit, before/after a critical operation, branch taken, state mutation) chosen by unaided judgment, its NDJSON payload has no correlation field, and the Stuck-Loop Checkpoint emits per-hypothesis verdicts rather than a reconstructed data path. So a bug whose suspect data crosses frontend to backend to a sub-agent has no notion that those hops are boundaries worth probing, no way to prove two probes concern the same request, and no report showing where a value first went wrong. Three additions, all confined to Phase 4 and the Checkpoint: a discovery-only boundary inventory (HTTP route/handler definitions, outbound HTTP client call sites, queue publish/consume points, sub-agent spawn sites) that does NOT authorise a probe — the existing no-orphan-probe hypothesis gate still decides which listed boundaries get instrumented; a W3C Trace Context `traceparent`-shaped correlation field (shared trace-id, per-probe span-id) adopted as a naming convention with no SDK and no dependency, including the payload-carried variant for queues, background jobs, and agent spawns where headers cannot travel, and the fragmented-trace case where a hop drops the value; and a path-reconstruction step that orders probes by trace-id then timestamp and names the first boundary where an observed value diverged from prediction, reporting `path incomplete` when probes exist on only one side rather than inferring the missing side. Two deliberate positions recorded rather than hidden. First, the user defined the Agent tier as a sub-agent's tool calls, which requires reading `memory/event-trace/`; this task splits write from read — writing debug output there stays forbidden per T058, reading it at analysis time as a correlation source is new and permitted, a stated amendment to a T058 decision. Second, **the auto-discovery direction has prior art against it**: TraceCoder (arXiv 2602.06875), the one system found that actually built this component, places probes by LLM reasoning over previous execution failures rather than static analysis, and the user chose static discovery with that counter-evidence stated — only the abstract-level claim was readable, so its reasons are unread. Mitigated by keeping the inventory discovery-only so hypothesis reasoning stays load-bearing; if Stage 3 or 4 finds the inventory motivating probes the hypotheses did not, that is the predicted failure and escalates rather than being worked around. Prior-art search 2026-08-07 confirmed nothing in the agent-skill space covers the cross-tier leg — doraemonkeys/claude-code-debug-mode is explicitly single-process with no correlation IDs, and Honeycomb's instrumentation-advisor does not publish its scanning method. **Also recorded: this repo cannot dogfood the feature** (Python and shell, no BE, no FE), so tests assert against instruction text and fixtures and must not be described as end-to-end. Blocked on T059 — any Stage 3 spawn runs in a worktree, where the destructive token-audit test reproduces a known data-loss incident | common-infrastructure | C2 | Risk: Medium | P1 | Stage 2 planned 2026-08-07 | **Unblocked 2026-08-07** — T059 merged, so a worktree spawn no longer reproduces the token-audit data-loss incident. Stage 3 spawned 2026-08-07 | Stage 3 completed 2026-08-07 in a pre-made worktree off the branch tip (no `isolation`). Phase 4 goes from 7 steps to 9: a discovery-only boundary inventory naming all four artifact classes, a `traceparent`-shaped correlate step with the payload-carried variant for queues/jobs/agent-spawns and the fragmented-trace case, and the payload gains the correlation field; the Checkpoint gains path reconstruction ahead of any verdict, grouping by trace-id and reporting `path incomplete` rather than inferring a missing side. The write/read split on `memory/event-trace/` is stated in step 1 as the deliberate T058 amendment. Stage 4: code-review 0 P0 / 0 P1 / **1 P2 fixed** / 1 P3 accepted — the P2 was `traceparent` added to the payload field list unconditionally while step 4 tells the empty-inventory single-process case to proceed 'unchanged', which would have required a degenerate correlation field on every probe of a run with no hop. **The first attempt at that fix passed the entire suite while asserting nothing** (mutating the new sentence stayed GREEN), the same gap T058's own Stage 4 P2 found, so it now carries its own mutation-verified assertion. The exact comma-separated payload field list was left byte-identical rather than reworded, because a test pins that string — weakening the assertion to fit the fix would have been the wrong direction. security-review **PASS, 0 actionable**, run manually and labelled (fifth occurrence of the built-in diffing the checked-out branch). 15 mutation controls RED→GREEN, AC12 injected three times separately since a negative substring check passes trivially otherwise; the Supervisor re-ran AC2 and AC7 itself and recomputed the Phase 1/2/3 body hashes and heading list against `1ac8cfa`, so the implementer was not the sole oracle. 13 new tests, **220 passed** + smoke PASS. **Recorded honestly: not end-to-end** — no BE and no FE in this repo, so every check asserts against instruction text and the first real exercise happens downstream | Completed: 2026-08-07
- [x] **T059** — a test writes to a tracked repo file, and in a worktree that destroys data. `.claude/hooks/tests/test_token_audit_generator.py:235` `test_real_report_generation_and_gitignore` requests the `tmp_path` fixture and then ignores it, calling `token_audit.generate_report()` with `ROOT / "reports" / "token-audit_2026-07-21.md"` as the output path — the real tracked file. In the main checkout `memory/event-trace/` is populated, so regeneration is roughly idempotent and only ever appends; the damage is invisible there beyond a permanently dirty working tree. In a worktree the trace dir is gitignored and therefore empty, so the same call writes an empty Entries block over all 106 derived entries. Found during T058's Stage 4 review 2026-08-06: the sub-agent's implementation commit had swept up the clobbered file, and re-running the suite reproduced it deterministically, proving it was the suite and not the agent. This also explains the `M reports/token-audit_2026-07-21.md` that was already dirty in this session's opening `git status` — the suite has been silently mutating a tracked file for some time. The fix must keep AC4/AC8's intent (the real report exists, has the DDR-0001 header, and is not gitignored) while never writing to it: assert against the existing file rather than regenerating it, or generate into `tmp_path` and assert the tracked file separately. Note the pre-existing recorded gotcha this instantiates — worktree-isolated files silently die if gitignored, and `reports/token-audit_*.md` is a deliberate tracked exception to the `reports/` gitignore precisely so it survives across worktrees | common-infrastructure | C1 | Risk: Medium | P1 | Registered 2026-08-06 from T058 Stage 4 | Stage 2 planned 2026-08-07; guide at tasks/TASK_GUIDE_T059.md; BEFORE captured on main at 04:03:48Z | Stage 3 spawned 2026-08-07 into a pre-made worktree off the branch tip (no `isolation`, since `isolation:"worktree"` forks from main). Fix is 3 lines: the `generate_report` call against the tracked report deleted, the test now reads the file, unused `tmp_path` removed. Stage 4: code-review 0 P0 / 0 P1 / 0 P2 / 3 P3 accepted (test name still says `generation` though it no longer generates; real-trace-dir shape coverage now rests entirely on the 8 synthetic `tmp_path` calls; a pre-existing unused `import os` deliberately left alone as out of scope under Surgical Changes). security-review **PASS, 0 actionable**, run manually and labelled — the built-in diffs the checked-out branch, which was not `t059-work`, so invoking it would have returned a false PASS (fourth occurrence of that failure mode); the diff removes a filesystem write and adds no input handling, so attack surface strictly decreases. **The Supervisor re-ran the SC2 mutation control itself rather than trusting the agent's**: truncated report → `1 failed`, `cp` restore → `1 passed`, so the implementer was not the sole oracle. Full suite `207 passed` with `git status --short` empty inside the worktree — the destructive environment is where that matters | Completed: 2026-08-07
- [x] **T058** — diagnose — evidence-driven instrumentation loop (NDJSON logs, hypothesis-tagged). Phase 4 is currently one line ranking instrument types by preference; it never says what to log, in what format, how many probes, how a probe ties to a hypothesis, or when to stop. Rewritten into a ≥5-step procedure and, because that loop exposes them, three downstream holes are closed in the same change. Direction locked with the user 2026-08-06 after brainstorming and grilling: no new skill, no hard-bug/easy-bug split, and the earlier C0–C3 phase-gating idea explicitly **dropped, not deferred** (a second scaling mechanism would compete with the Complexity matrix, and complexity is not knowable before the bug is diagnosed). Debug logs are the agent's own temporary instrumentation and must never be routed to `memory/event-trace/` — the user ruled that channel out as serving the merge gate and token-audit generator, and it records tool calls rather than program values. **Raised C1→C2 mid-planning** after the user pointed at prior art, `github.com/millionco/debug-agent` (MIT), whose skill is a mature implementation of this exact idea: eight mechanisms adopted — NDJSON one-object-per-line logs, a mandatory `hypothesisId` on every probe (which makes T052's Stuck-Loop counting mechanical rather than informal), CONFIRMED/REJECTED/INCONCLUSIVE verdicts with cited log lines, `#region debug log` markers superseding the `[DEBUG-xxxx]` prefix for deterministic cleanup, a 1–10 probe budget, retain-instrumentation-until-post-fix-verification, revert-changes-from-REJECTED-hypotheses, and a secrets/PII prohibition. Its transport layer is rejected outright (Node daemon, HTTP ingest server, hosted remote relay) — wrong dependency for a Python/shell repo and an external hop for program values. Where ours is stronger it is kept: the Phase 1 ten-rung repro ladder stays ahead of any ask-the-user step, and the T052 checkpoint has no counterpart in the prior art. AC12/AC13/AC14 are negative criteria pinning line 8, the full `### Phase` heading list, and the Phase 1/2/3 bodies byte-identical, so neither the dropped gating direction nor scope creep into the repro ladder can re-enter through the implementation | common-infrastructure | C2 | Risk: Medium | P1 | Stage 2 planned 2026-08-06 | Stage 3 spawned 2026-08-06 into a pre-made worktree off the branch tip (no `isolation`, since `isolation:"worktree"` forks from main). The agent completed the work and flagged one out-of-scope edit itself: line 13's Karpathy Surgical-Changes override still pointed at the retired `[DEBUG-xxxx]` prefix, a third dangling reference the guide's own AC table failed to enumerate — approved, since leaving it would have left two competing cleanup contracts in one file. **The Supervisor caught one thing the agent did not report**: its implementation commit had swept up a regenerated `reports/token-audit_2026-07-21.md` that had lost all 106 derived entries. Reverted, then reproduced deterministically by re-running the suite, which proved the cause was not the agent but `test_token_audit_generator.py` writing to the real tracked file — registered as **T059**. Stage 4: code-review 0 P0 / 0 P1 / 1 P2 fixed / 2 P3 accepted; the P2 was three constraints the guide's own Approach called load-bearing (perf sub-case, no-seam fallback, one-variable-at-a-time) that had no assertion pinning them. security-review **PASS, 0 actionable**, run manually and labelled — the built-in diffs the checked-out branch, which was not `t058-work`, so invoking it would have returned a false PASS (third occurrence of that failure mode); 1 P3 noted, NDJSON logs land in a world-readable temp dir on shared machines. All six mutation controls observed RED then restored (three by the implementer, three by the Supervisor for the assertions it added itself, so the implementer was not the sole oracle for its own tests). Evidence was refreshed at Stage 5 — it recorded 16 tests / 204 passed, captured before the P2 fix, and the merge gate scans those exact cells. 19 new tests, **207 passed** | Completed: 2026-08-06
- [x] **T057** — A sub-agent inherits its parent's `session_id`, so T056's session-scoped step counter does not separate an agent from its Supervisor. Observed 2026-08-06 on T054: the agent legitimately spent 42 calls on a ~7-file task and the counter `step_count_<parent-session>_T054.txt` blocked the Supervisor's own session too, requiring a 4th manual user reset in one session. T056 fixed agent-vs-unrelated-session bleed; this is agent-vs-parent, which is the pairing that actually occurs. Options to weigh: include the agent's own identity in the key, exempt the Supervisor's session from the guard, or raise/scope `CLAUDE_STEP_LIMIT` for multi-file C2+ tasks (40 was demonstrably too low here). Must also give a blocked session a reliable self-clear path — `Write` only escapes when the counter file does not yet exist | common-infrastructure | C2 | Risk: Medium | P0 | Stage 2 planned 2026-08-06. Approach locked by user: **self-clearing block** — on exceeding the limit, emit the block AND reset the counter in the same call, so a runaway is still interrupted but no session can be left unable to act and no manual `rm` is ever needed. Deliberately identity-free (no hook field currently distinguishes an agent from its spawner). Accepted trade-off, stated not hidden: a true loop is now interrupted every N calls rather than halted, mitigated by an escalating block-count message (AC6). Default limit also raised from 40 — T054 spent 42 calls making forward progress on every one. Stage 3 spawned 2026-08-06. Implemented: reset write happens **before** the block message is emitted, so a crash between them can never leave the counter above the limit; two-line counter (`count\nblock_count`) with legacy one-line files degrading to `block_count=0`; TTL expiry resets both (documented deliberate choice); default `CLAUDE_STEP_LIMIT` 40→90, justified in-comment from evidence (T054 spent 42 legitimate calls, T056 38); escalating message on the 3rd+ block; the false 'manually reset' instruction removed. **The agent stopped and reported 8 failing pre-existing tests rather than editing them green (AC9 working as intended).** The Supervisor verified each independently: all 8 encode intentionally-superseded behaviour — hardcoded 'blocks at 41' (broken by the raised default), the single-line counter format, and SC2's explicit 'session A must still be blocked' which this task deliberately reverses. Updated with inline reasons, each keeping the property it existed to protect; the limit is now pinned via env in that suite so a future default change cannot silently invalidate it again. Behaviour verified end-to-end by the Supervisor, not just asserted: block fires at the limit, **the next call is allowed** (session self-recovers), and AC5/AC6 confirmed live (no manual-reset wording; escalation appears on the 3rd block). Stage 4: security-review **PASS, 0 actionable** (only new surface is a write to the already-sanitized counter path). 188 passed | common-infrastructure | C2 | Risk: Medium | P0 | Completed: 2026-08-06
- [x] **T054** — delivery-report skill + HTML template + reminder hook + harness sync | common-infrastructure | C2 | Risk: Medium | P1 | Unblocked 2026-08-06 (T053 merged). Stage 3 spawned 2026-08-06 in a pre-made worktree off the branch tip. Note: this is the first task this session with live UI Evidence rows — Hard-Stop Gate 6 applies, and the Supervisor signs off the visual verdict, not the implementer. **Stage 3 run killed by the step-limit hook at 42 calls before its first commit** — the counter was session-scoped per T056, but a sub-agent inherits its parent's `session_id`, so the Supervisor was blocked too (see T057). Work recovered uncommitted from the worktree by the Supervisor, who committed it, re-ran the suite to check the agent's one unverified `stop_review_reminder.py` edit (177 passed), and completed the CLAUDE.md/CLAUDE_LEGACY.md registration the agent never reached. MANIFEST deliberately unchanged — it lists directories copied with `cp -r`, so the new skill and template already deploy; explicit paths would be redundant and break its convention. AC1 verified (no flavor-specific branch in the parser); **AC7 verified the strong way** — rendering T053, whose guide contains a typed WITNESS paragraph, correctly reported `WITNESS underived`, refusing to launder a typed claim into evidence. Stage 4: code-review scoped to 6 files; security-review **PASS, 1 P3** (`task_id` unsanitized into a read path — operator-invoked CLI, no untrusted input, not actionable now). UI sign-off by the Supervisor, not the implementer, per the guide: 0 external assets, palette matches `report_template.html`, `max-width:980px` + `flex:1 1 320px` wrap so no horizontal scroll at 1024px+. Real render produced a 9380-byte page with zero unfilled slots and `6 / 9 filled` over T053's Evidence table | common-infrastructure | C2 | Risk: Medium | P1 | Completed: 2026-08-06
- [x] **T053** — Demonstration block in both guide flavors + spawn-time blank-BEFORE warning. Adds a 4-field `Demonstration` section (BEFORE / AFTER / DELTA / WITNESS, no N/A path on BEFORE) to `templates/TASK_GUIDE_template.md` and to the bugfix Step 3 skeleton, wires the BEFORE-capture-before-implementation instruction into `craft-spawn-prompt`, and adds a non-blocking blank-BEFORE warning to `pre_agent_validate_guide.py` in the same shape as its existing `Depends on` advisory. Implements DDR-0003 decisions 1, 2, 5. AC8 explicitly re-confirms the new H2 doesn't perturb any existing field-anchored guide parse — the 7th guard against this hook family's recorded parsing-defect pattern | common-infrastructure | C2 | Risk: Medium | P1 | Stage 3 spawned 2026-08-06 (worktree-isolated). **Stage 3 run killed by the step-limit hook after 40 calls**, having completed only the 3 markdown changes; the Supervisor implemented the remaining AC6/AC7 hook advisory and all 8 tests. Stage 4: 1 self-review defect found and fixed before commit (`value.replace("OR","")` stripped those letters from any word, e.g. "ERROR"→"ERR" — narrowed to `\bOR\b`); security-review PASS, 0 actionable. Mutation-verified: missing-section branch `True`→`False` → RED `1 failed, 158 passed`, restore → GREEN `159 passed` | Completed: 2026-08-06 — Stage 4 closed, Stage 5 verified from the main checkout post-merge (169 passed). **Merged into `docs/stage2-demonstration-block-t053-t055` and pushed; NOT merged to main, no PR open**
- [x] **T055** — bugfix Evidence-table parity with the gate-visible implementation shape. The bugfix flavor's 3-row Evidence table has no `verify` row at all, so the merge gate is not failing on bugfix tasks — it is structurally absent. Expands the table to the implementation flavor's row set and `☐ pass / ☐ fail` cell shape while preserving the 3 bugfix-specific rows, and updates Step 5's review-gate wording. Implements DDR-0003 decision 6. SC3 is the load-bearing case: "pass" in the Result column but not the Notes column must stay caught, since that is the exact T026 defect and it looks correct to a human reader | common-infrastructure | C1 | Risk: Medium | P1 | Stage 3 spawned 2026-08-06 (worktree-isolated) — known textual conflict with T053 in `.claude/skills/bugfix/SKILL.md`; Supervisor resolves at merge. **Stage 3 run killed by the step-limit hook mid-task** (shared `.state/active_task` race — see T056); work recovered from the worktree, integrated and completed by the Supervisor. Stage 4: scope bounded to 2 files + tests; security-review PASS, 0 actionable (test-only `subprocess`/`exec`, no network, no secrets, no new external input). Mutation-verified independently rather than trusting the agent's report: `verify`→`verifyX` in the skeleton → RED `1 failed, 158 passed`, restore → GREEN `159 passed` | Completed: 2026-08-06 — Stage 4 closed, Stage 5 verified from the main checkout post-merge (169 passed). **Merged into `docs/stage2-demonstration-block-t053-t055` and pushed; NOT merged to main, no PR open**
- [x] **T056** — Session-scoped step counters + TTL expiry. The step-limit guard hard-blocked a whole Supervisor session three times on 2026-08-06: counters were keyed on task ID alone and are **never** auto-reset (`post_agent_move_to_review` is deliberately inert since T044), so one session's exhausted 40-call budget broadcast to every session resolving to that task — including the Supervisor's, whose `Bash` and `Read` were both killed while the hook's own message told it to run a reset that itself needs a tool call. The third incident's poisoning counter belonged to T053, a task already Done and pushed hours earlier. Fix: counter key is now `step_count_<sanitized-session>_<task>.txt` off the event's `session_id`, plus mtime-based TTL (`CLAUDE_STEP_COUNT_TTL_S`, default 6h) treating an expired counter as 0; `makedirs`/write wrapped so an unwritable state dir fails open; block message names the exact session-scoped file. `session_id` is treated as untrusted (reaches a filename): sanitized to `[A-Za-z0-9]`, truncated to 64, falls back to `nosession`. Attribution/`task_context.py` deliberately untouched — this guard is now safe without depending on that race being fixed. Stage 4: code-review — the 2 pre-existing test edits are filename-literal updates, assertions unweakened; security-review **PASS, 0 actionable** (manual: 11 hostile `session_id` probes incl. `../../../etc/passwd`, `$(whoami)`, NUL, 200-char — no traversal, injection, empty or overlong segment). Incident reproduced end-to-end against the fixed hook by the Supervisor, not just asserted in tests: 41 events as session A → blocked, 1 event as session B → **allowed**. Mutation-verified: dropping the session key → SC2 RED; disabling the TTL → SC3 RED; both restored GREEN. 169 passed | common-infrastructure | C2 | Risk: Medium | P0 | Completed: 2026-08-06
- [x] **T030** — Post-baseline analysis of the token-audit windows found the instrument itself was broken on a second axis: `/cost` ground-truth was never pasted into either window's report (window 1 or window 2), so DDR-0001's ≥20%/<5% $/task criteria are structurally unanswerable — no dollar figure was ever attached to any entry. Separately, window 1's report wasn't actually window-scoped (105 entries span 2026-07-14→2026-08-04, unfiltered trace history, not the reopened window's 4/7-session slice memory recorded). Independent of the broken instrument, both candidate refactors DDR-0001 was gatekeeping had **already shipped for unrelated reasons**: Option A (CLAUDE.md trim) via T049 (565→198 lines, readability-motivated), Option C (slim-skills) via T029. Nothing was left to decide against a $/task threshold even if the data had existed. User chose "retire, don't re-instrument" over a 3rd measurement attempt or hunting for external `/cost` records. Wrote [DDR-0002](docs/ddr/0002-retire-measure-first-token-instrument.md) superseding DDR-0001 (2-of-3 gate: surprising-without-context + genuine trade-off); DDR-0001's Status line updated to point at it. `reports/token-audit_*.md` files retained as historical record of both failure modes; `scripts/token-audit.sh`/`token_audit.py` left in place but no new window opens. This is a Supervisor+user HITL analysis/decision task, not implementation — no code changed, no TASK_GUIDE required (decision-artifact work follows the same direct-write precedent as DDR-0001/T027) | Supervisor + user (HITL) | C1 | Risk: Low | P1 | Completed: 2026-08-05
- [x] **T052** — In bugfix/diagnosis sessions the agent tried hypothesis after hypothesis with no stopping point, no options ever surfaced, no warning — the user's real pain point. Added `### Stuck-Loop Checkpoint (mandatory)` to `diagnose` (Phase 4→5 boundary): after **2 consecutive disproven hypotheses**, STOP before testing a 3rd — present exactly 3 named options (try next hypothesis / widen scope-reconsider mental model / abandon+escalate to Supervisor), explicit consecutive-not-total wording, explicit standalone-diagnose handling (report to Supervisor directly when no bugfix-shaped TASK_GUIDE exists — never silently skip). Added `### Attempts Log` to `bugfix`'s Step 3 TASK_GUIDE template (positioned after Diagnosis Gates, before Fix Gates) so the hypothesis trail + checkpoint outcome is captured in the plan, not just a chat aside — directly per user's "concerns should be in the plan to check or evaluation" instruction. Hard threshold of 2 chosen over a judgment call specifically because judgment-call self-monitoring was the exact failure mode being fixed (tracked as a reversible Supervisor default, user deferred). Brainstorming (3 paths: soft skill-only / hard trace-hook / TASK_GUIDE-only) converged on skill-level checkpoint + TASK_GUIDE capture combined; hard trace-hook explicitly rejected (heuristic "failed attempt" detection from trace data too false-positive-prone). Not a DDR (doesn't clear 2-of-3 criteria), recorded in `memory/decisions.md` only. Stage 4: code-review 0 findings — pure insertion in both files (24 lines, 0 removed), diagnose Phases 1/2/5/6 byte-identical; security-review not mandated (Low risk, pure skill-instruction text). 1 P3 advisory noted (bugfix.md was already 160 lines pre-task, past the ~150 slim-skills convention — the 9-line addition to 169 is a reasonable necessary increase, not new bloat). All verification independently re-run by Supervisor (case-insensitive grep matches all 4 required phrases — the guide's literal-case grep pattern doesn't match sentence-initial capitalization, cosmetic script mismatch not a content defect); full suite 139 passed | C1 | Completed: 2026-08-04
- [x] **T051** — Codex CLI doesn't auto-generate `AGENTS.md`, requiring the user to manually prompt it each session; `docs/MULTI_AGENT.md` had sketched a root `AGENTS.md` under "Optional" but never shipped it. Created `AGENTS.md` (12 lines, 4 base-rule bullets + explicit "CLAUDE.md and .claude/agents/ remain canonical" pointer — deliberately no Karpathy Principles table or Hard-Stop Gates, to avoid becoming a second source of truth). `MANIFEST` gained an `AGENTS.md` line (user-approved scope: deploys to downstream projects too, not repo-local). `general-agent-template.md` gained a Staleness Guard footer note. `docs/MULTI_AGENT.md`'s section renamed "Optional" → "Shared" and updated to point at the real file. Brainstorming (3 paths: static/generated/skip) converged on static — a ~15-line file doesn't justify generator machinery; grill-with-docs confirmed MANIFEST scope; DDR/ADR check didn't clear 2-of-3, recorded in `memory/decisions.md` only. Stage 4: code-review 0 findings (diff scoped to exactly the 4 predicted files); security-review not mandated (Low risk, docs-only, no injection surface). All 4 verification checks independently re-run by Supervisor: pass; negative case confirmed (no Karpathy/Hard-Stop text leaked into AGENTS.md); full suite 139 passed | C1 | Completed: 2026-08-04
- [x] **T050** — `scripts/token_audit.py` had `WINDOW_DATE`/`DEFAULT_REPORT_PATH` hardcoded and `build_entries()` derived from every record in `memory/event-trace/*.jsonl` with no lower-bound filter — rerunning it for a new window would re-derive the entire trace history instead of starting fresh. Added optional `window_start` param (default `None` preserves original unfiltered behavior, AC1) plus `argparse` CLI flags (`--window-start`/`--report-path`/`--trace-dir`, env-var defaults kept for back-compat) to both `token_audit.py` and the `token-audit.sh` wrapper (`"$@"` passthrough). A record with an unparseable date (`_date_from_timestamp` returns `"?"`) is excluded with a stderr warning when a window filter is active, never silently included or silently dropped. Generated `reports/token-audit_2026-08-04.md` for the new window. Stage 4: code-review 0 P0/P1/P2/P3 (Supervisor-run, diff scoped exactly to predicted files); security-review not mandated (Low risk) — quick manual check found no injection surface (report_path/window_start are trusted CLI/env inputs, no eval/exec). 7 new tests (`.claude/hooks/tests/test_token_audit_window_filter.py`) independently re-run by Supervisor: 7 passed; full suite 146 passed | C1 | Completed: 2026-08-04
- [x] **T049** — CLAUDE.md was 565 lines; split into `docs/claude-md/{folder-structure,code-naming-conventions,phase0-project-initiation,pipeline-stages,memory-write-protocol}.md`, linked back from short pointers that keep each stage's `Skill({...})`/`Agent({...})` calls inline. Hard-Stop Gates, Karpathy Principles table, and Mandatory Session Startup deliberately stayed inline (highest read-frequency, safety-critical). `MANIFEST` gained `docs/claude-md` (directory entry, same pattern as `.claude/skills`) so setup.sh/update.sh deploy the linked files — a CLAUDE.md link pointing at a file that doesn't exist post-install would have been a silent regression for every downstream project. `CLAUDE_LEGACY.md` untouched (separate follow-up, has its own additions-only sync policy). Stage 4: code-review 0 P0/P1/P2/P3 (Supervisor-run, docs-only diff, content-preservation confirmed section-by-section against the pre-task file); security-review PASS 0 findings (pure Markdown/MANIFEST diff, no executable surface). Evidence independently reproduced by Supervisor: `wc -l CLAUDE.md`=198, all 5 links resolve, `git diff --stat CLAUDE_LEGACY.md` empty, 139/139 hook smoke suite green (unaffected, no hook code touched) | C2 | Completed: 2026-08-04
- [x] **T045** — Two Kanban-parsing hooks (`find_kanban_section`, `tasks_in_section`) used an unanchored `(?=###|\Z)` lookahead, so a literal `###` quoted inside a row's text (real 2026-07-23 incident: T039's Done row) truncated the section early and hid every row below it. Anchored to `(?=^###|\Z)` + `re.MULTILINE` in both hooks — 5th defect in this hook family (T018/T022/T024/T042/this). Stage 4: code-review 0 P0/P1/P2 (manual, sub-agent has no Skill tool); security-review manual PASS 0 findings (Supervisor-run, this repo's `origin` remote works from a checked-out feature branch). 8 new tests reproduce the real board shape; mutation control (revert anchor) observed RED, restored. Full suite 139 passed + smoke PASS, independently re-run by Supervisor | C1 | Completed: 2026-08-03
- [x] **T040** — DDR-0001's manual per-session Token Audit Log convention collapsed after 1 of 7 sessions (never once captured `/cost`). Replaced with `scripts/token-audit.sh` + `scripts/token_audit.py`, a generator (not a hook) deriving `spawn`/`cold-start`/`stage-N` entries from the existing `memory/event-trace/*.jsonl` (written by unmodified `post_tool_trace.py`); never synthesizes token counts, emits `?` for unknown model tier. Opened `reports/token-audit_2026-07-21.md` (0 entries — no trace data in this checkout yet, expected); old window's closed-inconclusive header left intact. Idempotent by regenerating the whole file each run. Stage 4: code-review 0 P0/P1/P2 manual PASS; security-review manual PASS 0 findings (Supervisor-run — no injection surface, all inputs are local trace files this repo already writes). 10 new tests; mutation control (disabled Agent→spawn branch) observed RED, restored. Full suite 139 passed + smoke PASS. **T030's blocker**: window reopens from 2026-08-03, still needs 7 sessions/14 real days of data — not resolvable by any code change | C1 | Completed: 2026-08-03
- [x] **T041** — `general-agent-template.md` commanded sub-agents to "strictly follow all Karpathy Engineering Principles" but never delivered the text (only in `CLAUDE.md`, deliberately excluded from the Mandatory Startup Sequence — too costly per spawn); separately, nothing told agents to check for existing code before writing new code. Inlined a compact 4-principle table (name + operational command) with a CLAUDE.md sync pointer, plus a new `## Search Before You Build` section (7 ponytail-order rungs + non-negotiables block: correctness/validation/error-handling/security/explicit-requirements never traded for a shorter diff). File grew 87→117 lines (within the 45-line/132 budget); no `CLAUDE.md` added to startup list; no "ponytail" string anywhere in `.claude/agents/**`; all 4 role guides checksum-verified byte-identical to HEAD. Stage 4: code-review 0 P0/P1/P2 manual PASS; security-review manual PASS 0 findings (docs-only). `scripts/test-agent-template.sh` written first, 4 negative controls (deleted rung, CLAUDE.md added, "ponytail" inserted, backend.md touched) all correctly failed then reverted. Full suite + smoke PASS, independently re-run by Supervisor | C2 | Completed: 2026-08-03
- [x] **T048** — The hook test suite failed whenever the active-task channel was armed. T047's state-file channel works, but it added a `StateFileOverride` isolation helper and applied it only to its own new tests; nine pre-existing T043-era tests read the real `ACTIVE_TASK_FILE` and assert what the *lower* precedence slots resolve to, so a real armed file made slot 2 short-circuit them. Production logic was correct — the damage was to the workflow: `pytest .claude/hooks/tests/` is the TASK_GUIDE template's verification command and the output Hard-Stop Gate 5 requires, so T047's two halves conflicted for every future task. Fixed by applying the existing helper suite-wide at both entry points (pytest autouse fixture + the `__main__` runner), wrapping each *test function* so slot-2 tests keep nesting their own armed override inside it. `conftest.py` was rejected for a real reason: this module loads `task_context` via `importlib.spec_from_file_location`, so a conftest-owned copy is a different module object and mutating it would be silently inert. Stage 4: 0 P0 / 0 P1 / 0 P2 / 1 P3 advisory; security-review PASS 0 findings (non-test diff empty). Verified across four ambient states (valid/malformed/stale/absent) → 121 passed each, plus direct execution; AC5 mutation independently reproduced by the Supervisor — disabling slot 2 turns 6 slot-2 tests RED, so isolation did not blind the suite | C1 | Completed: 2026-07-31 — **Merged to main 2026-07-31**
- [x] **T047** — `CLAUDE_ACTIVE_TASK` never reached the trace hook, so the merge gate failed closed on honest tasks. T044 tightened the gate *and* shipped AC7 to keep honest work passing, but AC7 was inert: hooks are spawned by the harness as siblings of the tool call, inheriting the harness env, never the subshell an `export` creates inside a Bash tool call — every record landed in `_untagged.jsonl`. Fixed with a structural, writable-from-a-tool-call channel: `.claude/hooks/.state/active_task` (task ID + ISO timestamp), added to `resolve_task_id`'s precedence after the env var, rejected when malformed/unreadable/stale (6h). **Two review rounds.** Round 1 P1: the shipped instruction wrote the file cwd-relative (the agent's worktree) while the live hook read it absolute off the main checkout — different files, so the fix didn't work for its only real caller; the implementer's own end-to-end check missed it because it ran inside one shared root. Fixed via `$CLAUDE_PROJECT_DIR` resolution, and while fixing it the agent found that variable is *empty* inside an agent's Bash tool call, so instructing agents to use it verbatim would have shipped a symmetric defect — the instruction now embeds a literal absolute path at prompt-assembly time. Round 2 P1: `ACTIVE_TASK_MAX_AGE_S` parsed with a bare `int(os.environ...)` at import, below the never-raises contract — a `6h` value killed attribution repo-wide including the unrelated path-field slot; reproduced empirically, fixed at `41f4c14`. Stage 4: 0 P0 / 1 P1 fixed / 0 P2 / 0 P3; security-review manual — PASS, 0 actionable (injection suite against the task-ID validator all rejected). 121 tests (1 added by the Supervisor at review), all mutation controls observed RED then reverted, the boundary-straddling control independently reproduced by the Supervisor | C1 | Completed: 2026-07-31 — **Merged to main 2026-07-31**
- [x] **T044** — Hook lifecycle & evidence integrity — make the merge gate mean something. Three defects in one subsystem: the review-move hook took the task ID from a free-text scan of a spawn prompt that always carries a verbatim MEMORY.md paste (one spawn emptied In Progress and deleted 3 counters); it fired at spawn issuance, not completion; and the merge gate's evidence check was a bare substring match satisfied by any command merely *mentioning* a test runner. Fixed: structural attribution via T043's `resolve_task_id`; the move hook is now deliberately inert and documents why (`SubagentStop`/`TaskCompleted` carry no task identity, so no correct trigger exists); `trace_shows_verification` now matches a runner **anchored at a command head** with quoted spans treated as data. `craft-spawn-prompt` gained the mandatory `CLAUDE_ACTIVE_TASK=Txxx` export so the tightened gate doesn't fail closed on honest tasks. Stage 4: code-review 0 P0 / 1 P1 (the P1 was one layer *below* the new matcher — `extract_command`'s truncated-JSON fallback leaked the agent-authored `description` field, reinstating the defect through a side door; fixed at `832231d`) / 0 P2 / 2 P3 accepted. security-review manual (built-in diffs the checked-out branch, wrong instrument here) — PASS, 0 actionable. 42 tests (2 added at review by the Supervisor so the implementer wasn't its own sole oracle), all mutation controls observed RED then reverted, verification independently re-run: 109 passed + smoke PASS | C2 | Completed: 2026-07-31 — **Merged to main 2026-07-31**
- [x] **T046** — Add advisory `**Pattern reference**:` field to `templates/TASK_GUIDE_template.md` `## Approach` — "match the existing style" now reaches a sub-agent as a concrete grep-able file pointer instead of a `CLAUDE.md` principle it never sees (the "already-covered must reach-the-context" learning). Sourced from a prompt-library scan (52 prompts; gap #1 of 3, gaps #2 UI self-check + #3 steering rejected as speculative). Advisory only — no hook/gate/backfill; mirrors `Depends on`/`Entry point` shape with an opt-out. Stage 4: 0 P0/P1/P2, 3 P3 (all test-file). security-review ran (2nd success in project history) — no findings. Hub-file claim verified by differential test: all 6 guide regexes + merge gate byte-identical pre/post. All 4 negative controls independently reproduced RED by Supervisor. 9 new tests, 69/69 suite green | C1 | Completed: 2026-07-24
- [x] **T043** — Fix trace/step-limit task attribution — both hooks took the first `T\d{3}` substring in the tool payload (the trace hook scanning `tool_response` too), so merely *reading* a file whose body mentions a task ID filed the record under it, and an Edit whose prose mentioned an old task ID counted a step against it. Now a shared `lib/task_context.py:resolve_task_id()` resolves structurally: validated `CLAUDE_ACTIVE_TASK` → guide path in a path-valued field → `Agent` spawn prompt → unattributed. Fail-open preserved. 29 new tests (18 subprocess from a foreign cwd); all 6 negative controls observed RED; Supervisor independently reproduced the defect fix and one mutation. Stage 4: 0 P0/P1, 3 P2 (2 out-of-scope, 1 mine), 1 P3. **security-review ran for the first time in project history** — no findings | C2 | Completed: 2026-07-23
- [x] **T039** — Dedup the `## Skills vs Agents` section in CLAUDE.md — 580→536 lines; kept only what the harness does not auto-inject (mechanism, subagent_type→file mapping, spawn-pointer note, blast-radius disambiguation, names-only stage index). Stage 4: 1 P0 (false `verify` Evidence claim — post-commit run actually failed) + 2 P1 (test compared against floating HEAD so it could never pass once committed; AC5 checksum was vacuous — the matcher anchored on an H2 prefix but the real heading is H3, so both sides extracted empty strings and compared equal). All fixed, negative control independently reproduced by Supervisor. security-review manual (built-in unrunnable: hardcodes `origin/HEAD`, remote is `github`) — no findings | C2 | Completed: 2026-07-23
- [x] **T042** — Fix post_write_register_task.py Complexity/Risk/Priority extraction — regexes never matched the template's own `**X Level**:` format, so every auto-registered row silently defaulted to C1/Low/P1; defaults now `?`; 4th regex defect in this hook family. Stage 4: 0 P0/P1, 1 P2 fixed, 3 P3 accepted. security-review unrunnable (built-in hardcodes `origin/HEAD`, remote is `github`) — done manually, no findings | C1 | Completed: 2026-07-21
- [x] **T038** — Fix setup.sh piped `curl \| sh` install — primary documented install command was completely broken since T031 (2 days, undetected until a real user hit it); verified against the real live pushed repo | C2 | Completed: 2026-07-19
- [x] **T037** — Fix CI shellcheck SC1091 — missing `-x` flag meant shellcheck refused to follow the already-correctly-annotated lib/harness-fetch.sh source, failing CI on an info-level finding | C0 | Completed: 2026-07-19
- [x] **T036** — Fix scripts/smoke-install.sh for direct-repo model — CI was broken (red) since T031-T033 landed, never caught until this review; 1 P1 code-review fix (vacuous assertion) | C1 | Completed: 2026-07-19
- [x] **T029** — Prune 4 oversized SKILL.md files: learn 182→128, map-codebase 165→130 pruned; bugfix + code-review skipped at safe floor. Recovered from a stale branch after the original prune was lost to a failed /compact | C1 | Completed: 2026-07-19
- [x] **T034** — QA — independent smoke suite for install/update (9/9 pass, incl. negative-control sabotage self-check on update.sh's conflict detection) | C1 | Completed: 2026-07-19
- [x] **T035** — README.md — install/update sections rewritten for direct-repo model (2 stale-doc gaps found beyond original scope: pack ~/.supervisor no-longer-auto-created, obsolete git-submodule note removed) | C0 | Completed: 2026-07-19
- [x] **T026** — Fix TASK_GUIDE_template.md example verify Evidence row (2 compounding gate bugs found + fixed) | C0 | Completed: 2026-07-19
- [x] **T024** — Fix post_write_register_task.py agent-field regex | C0 | Completed: 2026-07-19
- [x] **T012** — Registration — CLAUDE.md + MEMORY.md + README | C0 | Completed: 2026-07-19 (audit-confirmed: skills deploy via MANIFEST's blanket `.claude/skills` line, documented in CLAUDE.md/README.md/MEMORY.md — no separate registration work remained)
- [x] **T011** — Core `wake` SKILL.md — live 4-section cold-start briefing | C2 | Completed: 2026-07-19 (audit-confirmed: `.claude/skills/wake/SKILL.md` exists, mandatory-invoked every session per CLAUDE.md)
- [x] **T010** — Registration — CLAUDE.md + MEMORY.md + README + setup.sh | C0 | Completed: 2026-07-19 (audit-confirmed, same basis as T012)
- [x] **T009** — Core `learn` SKILL.md — detection, LR writing, supersession, skill promotion | C2 | Completed: 2026-07-19 (audit-confirmed: `.claude/skills/learn/SKILL.md` exists, actively used — LR-0001/LR-0002 on disk)
- [x] **T008** — Infrastructure — `memory/learning-records/` folder + LR format template | C0 | Completed: 2026-07-19 (audit-confirmed: folder exists with LR-0001, LR-0002)
- [x] **T007** — QA — Unit Tests + Sample Traces + Smoke Suite | C2 | Completed: 2026-07-19 (closed per user confirmation — original Typer-CLI-project scope, superseded when repo pivoted to the Supervisor framework; no code ever existed to test)
- [x] **T006** — Mermaid Export (Stretch Goal) | C1 | Completed: 2026-07-19 (closed per user confirmation — same superseded original scope as T005/T007)
- [x] **T005** — CLI Wiring — Typer Entrypoint | C1 | Completed: 2026-07-19 (closed per user confirmation — no Typer/Python CLI code ever existed in this repo; original scope superseded by the Supervisor framework direction)
- [x] **T028** — Token Audit Log — scaffold + entry convention + format test (DDR-0001) | C1 | Completed: 2026-07-19
- [x] **T033** — New update.sh — hash-lock compare, per-file conflict prompt, symlink-refusal, git-repo check (ADR-0001) | C2 | Completed: 2026-07-17
- [x] **T032** — Rewrite setup.sh — direct-copy install, git-repo prerequisite check, write .claude/harness-lock.json (ADR-0001) | C2 | Completed: 2026-07-17
- [x] **T031** — lib/harness-fetch.sh — shared temp-clone-copy-discard + MANIFEST parsing (ADR-0001) | C2 | Completed: 2026-07-17
- [x] **T027** — DDR (Design Decision Record) — default decision artifact, ADR as rare escalation | C1 | Completed: 2026-07-16
- [x] **T025** — craft-agent skill (optional, supplemental agent-drafting) + Stage 1.5 wiring | C2 | Completed: 2026-07-16
- [x] **T001** — setup.sh + MANIFEST | C1 | Completed: 2026-06-10
- [x] **T002** — update.sh | C1 | Completed: 2026-06-10
- [x] **T003** — README.md | C0 | Completed: 2026-06-10
- [x] **T004** — .gitignore + repo hygiene | C0 | Completed: 2026-06-10
- [x] **T015** — ui-test skill (easy-ui-mcp orchestration) | C1 | Completed: 2026-07-01
- [x] **T016** — Wire ui-test into pipeline (qa.md + CLAUDE.md) | C1 | Completed: 2026-07-01
- [x] **T021** — Clean dangling T013/T014 prose references in cold memory | C0 | Completed: 2026-07-14
- [x] **T022** — Harden pre_agent_validate_guide.py task-ID extraction | C2 | Completed: 2026-07-14
- [x] **T023** — craft-spawn-prompt skill + wire Stage 3 / bugfix to it | C2 | Completed: 2026-07-14
- [x] **T017** — Task dependency declaration & entry-point reachability check | C1 | Completed: 2026-07-07
- [x] **T018** — Fix title-extraction regex in post_write_register_task.py | C1 | Completed: 2026-07-14
- [x] **T019** — Reconcile PROJECT_KANBAN.md rows (T005-T012 metadata, remove T013/T014) | C1 | Completed: 2026-07-14
- [x] **T020** — Update .gitignore for live-instance tracked files | C1 | Completed: 2026-07-14

---

## Blocked

_None currently._

---

## Stage Tracker

| Stage | Status |
|-------|--------|
| 0.5 Brainstorming | ✅ Done |
| 1 Environment Setup | ✅ Done |
| 1.5 Sub-Agent Architecture | ✅ Done |
| 2 Planning (/plan) | ✅ Done |
| 3 Execution | ✅ Done |
| 4 Review | ✅ Done |
| 5 Integration & Verify | ✅ Done |
